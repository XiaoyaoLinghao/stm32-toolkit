# Historical OpenClaw Records

This directory is retained only as immutable project-history evidence for earlier STM32 Toolkit
work orders and implementation returns. It is not a current implementation entry point, ownership
source, task dispatcher, validation workflow, or remote-operation authority.

Current work follows the repository `AGENTS.md`, the approved specification under
`docs/superpowers/specs/`, and the active vertical plan under `docs/superpowers/plans/`.
