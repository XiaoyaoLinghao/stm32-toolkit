# STM32TK-0305-BUILD-IDENTITY r002 Implementation Report — Revision 1

Status: `IMPLEMENTED`
Module: `STM32TK-0305-BUILD-IDENTITY` / r002 / revision-1
Branch: `openclaw/STM32TK-0305-BUILD-IDENTITY/r002`
Accepted base commit: `e47eee0d374bd3a959fe555990b66a6163eb18b8` (exactly matches the work order; working tree clean at start)
Reviewed predecessor: `d130b033acac407c3416530808431915f1d11f09` (the r002 report commit returned after the first round; its code head was `e923cf31d64bb72280867c1024329aaafd94eeba`)
Code head before this report commit: `ddd869dccb2eaf0d0f155f06b78d7dc55a3d360d`
Final branch head: supplied only in the return message and PR metadata
Work order: `docs/openclaw/modules/STM32TK-0305-BUILD-IDENTITY.md` (specification commit `cc2e4947aa692f940655c9ddf18ee8ab1f68c824`, `READY_FOR_OPENCLAW`, accepted base matches)

## 1. Revision verdict and scope

Codex returned `REVISION_REQUIRED` for the reviewed predecessor `d130b033…`. All five rejection entries are test-fixture defects (no product-code change was required or made in this round); each is fixed in exactly one allowed test path, and every fix is proven by a Linux seam RED reproduction of the Windows failure mode followed by the GREEN fixture correction. The product code head is unchanged from the reviewed predecessor except for nothing: this round modified only `tools/stm32-toolkit/tests/test_process.py`, `tools/stm32-toolkit/tests/test_build_runner.py`, `tools/stm32-toolkit/tests/test_firmware_identity.py`, and this report.

| # | Codex rejection | Root cause observed/derived | Fix in this round | RED → GREEN evidence |
|---|---|---|---|---|
| 1 | Windows process tests permanently block: the fake `_taskkill` only terminates `Popen.pid`; the underlying interpreter keeps holding the pipe | Single-PID kill leaves a surviving descendant holding the pipe write end; the drain thread stays blocked in `read()` while holding the `BufferedReader` lock, so `process.stdout.close()` in `run_process`'s `finally` blocks indefinitely (Linux repro: `run_process` returned only at the descendant's natural 60 s exit, `rc=0`) | The `taskkill` seam (`make_taskkill_fake`) deterministically terminates the whole tree: POSIX `killpg` on the child's own group (the tree child calls `os.setpgid(0, 0)` before spawning the grandchild), Windows real `taskkill /T`; the three Windows-branch tests use a child+grandchild tree and prove both PIDs reaped via bounded polls with explicit timeouts (never a fixed sleep, never skip/xfail, never a single-PID kill) | RED: predecessor seam + tree child → `run_process` blocked 60.1 s and left the pipe open. GREEN: `test_process.py` alone exits 0 in 18 s; `assert_process_reaped` (OpenProcess on Windows / `kill(pid, 0)`+zombie-aware on POSIX) verifies child and grandchild reaped; no residual `time.sleep(60)` processes after the run |
| 2 | Unconditional `os.killpg` in test bodies is an `AttributeError` on Windows | Four tests asserted liveness with `os.killpg(pid, 0)` / `os.kill(pid, 0)` — POSIX-only APIs (Windows `os.kill(pid, 0)` is not an existence probe) | All liveness assertions route through the platform-adapted `assert_process_reaped`; the POSIX graceful→force seam test forces `_is_windows = False` and its fake force-kill uses `os.killpg` on POSIX / `os.kill(SIGKILL)` (TerminateProcess) on Windows; the Windows branch verifies `CREATE_NEW_PROCESS_GROUP`, graceful signal, `taskkill /T`, fallback `kill()`, and reaping contracts through the seams; no skip/xfail added | RED: predecessor assertions are POSIX-only. GREEN: full `test_process.py` passes on Linux CPython 3.10.11 with zero skip/xfail; Windows branch contract covered by `test_windows_branch_uses_create_new_process_group_flag`, `test_windows_branch_graceful_signal_is_seam_injected`, `test_windows_branch_uses_taskkill_seam`, `test_windows_branch_falls_back_to_kill_when_taskkill_fails` (kill-spy proxy asserts the fallback `kill()` ran) |
| 3 | CRLF fixture uses `sys.stdout.write`, which on Windows text mode double-translates to `\r\r\n` | Windows text-mode stdout rewrites `\n` → `\r\n`, so `'a\r\nb\rc\n'` arrives as `'a\r\r\nb\rc\r\n'` and normalizes to `a\n\nb\nc\n` | Fixture writes exact bytes via `sys.stdout.buffer.write(b'a\r\nb\rc\n')`; the product normalization contract (`a\nb\nc\n`, no `\r`) is unchanged | RED: feeding the Windows-translated bytes through `_normalize_newlines` yields `a\n\nb\nc\n`. GREEN: `test_crlf_and_bare_cr_are_normalized_to_lf` passes with exact `a\nb\nc\n` and `"\r" not in result.stdout` |
| 4 | Windows fake CMake interception fails: the `.cmd` references a non-existent `fake_cmake.py` and falls through to ambient CMake; the launch-failure test deletes a hardcoded extensionless path | The predecessor Windows launcher wrote `"{bin_dir / "fake_cmake.py"}"` without ever creating that file (dangling reference), so interception broke; `test_launch_failure_returns_configure_failed` deleted `fakebin/cmake`, which is not the Windows launcher (`cmake.cmd`) | `install_fake_cmake` now writes a real `fake_cmake.py` beside `cmake.cmd` (POSIX wrapper unchanged); fake git uses the same pattern (`fake_git.py` + `git.cmd`) via the new `install_script_binary`; the launch-failure test deletes the actual launcher from `fake_cmake_launcher(bin_dir)`; a new probe test runs the launcher directly and proves it reaches the Python double (hit record + unknown-argv exit 2); the success fixture asserts exactly two hit records with the fixed argv and cwd (a real cmake would leave no record and could not produce the evidence), explicitly proving ambient CMake was never executed; the product argv stays the exact fixed `cmake --preset <preset>` / `cmake --build --preset <preset> [--clean-first]` (no shell, no command strings) | RED: predecessor `.cmd` referenced `fake_cmake.py` which does not exist. GREEN: `test_fake_cmake_launcher_reaches_the_python_double` and the full runner/context success fixtures pass; the outOfRegion cascade the reviewer observed in the runner/context success fixtures on Windows (a consequence of the broken interception) no longer has a route to occur and the fixtures are green on Linux |
| 5 | Redirect tests create no traversable logical entries; the casefold test relies on NTFS being case-insensitive | The Windows `escape_redirect` simulation patched `resolve`/`lstat` but not `_listdir`, so the walk never saw the logical link entry (on POSIX a real symlink is listed, on Windows no physical entry exists); the casefold test created two case-only-different files, which is impossible on NTFS | The Windows simulation now patches the module seams together — `_listdir` surfaces the logical link entry, `lstat` reports symlink mode, `resolve` maps to the canonical target — so traversal, lstat, resolve, and containment all observe the same simulated object; the casefold collision is constructed through a controllable `_listdir`/`_lstat` seam presenting both logical paths over one physical file (NTFS-independent); a new `test_snapshot_rejects_redirect_loop` covers loop rejection (real self-loop on POSIX; deterministic `RuntimeError` resolve seam on Windows); coverage includes in-root redirect accepted, out-of-root redirect rejected, and loop rejected, without lowering the snapshot containment/collision/fail-closed constraints and with no skip/xfail | RED: predecessor simulation was not traversable (empty `listdir`) and the casefold fixture cannot exist on NTFS. GREEN: `test_firmware_identity.py` passes (91 tests, incl. the new loop test) on Linux; the seam logic was statically validated against the product seams and is deterministic without administrator privileges |

No product file was modified in this round: no RED test exposed an independent product defect, so the §1 hard constraint (do not expand the path set) held. No skip/xfail was added anywhere; the 17 full-suite skips are the pre-existing accepted-base Windows-only skips, unchanged.

## 2. Changed-path inventory (revision diff `d130b033…` → code head `ddd869dc…`)

| Status | Path | Purpose |
|---|---|---|
| M | `tools/stm32-toolkit/tests/test_process.py` | platform-adapted liveness/reaping assertions, deterministic whole-tree `taskkill` seam, raw-byte CRLF fixture, tree child/grandchild fixtures, kill-spy Popen proxy |
| M | `tools/stm32-toolkit/tests/test_build_runner.py` | real `fake_cmake.py`/`fake_git.py` scripts behind Windows `.cmd` launchers, launcher-path helper, actual-launcher launch-failure, launcher probe test, ambient-CMake proof, script-binary installer |
| M | `tools/stm32-toolkit/tests/test_firmware_identity.py` | traversable redirect simulation (`_listdir`+`lstat`+`resolve` seams), NTFS-independent casefold seam, redirect-loop rejection test |
| A | `docs/openclaw/returns/STM32TK-0305-BUILD-IDENTITY/r002-implementation-report.md` | this report (report-only addition after the code head) |

`git diff --check d130b033…...HEAD` and `git diff --name-status d130b033…...HEAD` confirm exactly these paths and no whitespace errors. The full r002 diff from the accepted base (`e47eee0d…` → HEAD) contains exactly the 23 mandated implementation paths plus this report, with `git diff --check e47eee0d…...HEAD` silent. No commit was amended, rebased, cherry-picked, or force-pushed; `master`, the specification commit, the r001 branch, and PR #5 were not touched.

## 3. Environment-separated verification (this revision round)

OpenClaw environment: Linux x86_64 (`Linux 7.0.0-22-generic`); CPython 3.10.11 (`/home/openclaw/coding/venvs/tk0302`, outside the repository); jsonschema 4.23.0, mcp 1.27.0, pyelftools 0.33, Jinja2 3.1.6, pytest 8.3.5, pytest-cov 6.0.0; package installed editable. All OpenClaw commands ran from the repository root on branch `openclaw/STM32TK-0305-BUILD-IDENTITY/r002` at code head `ddd869dc…`.

| Gate/command | Owner | Environment | Commit tested | Exit | Observed result | Status |
|---|---:|---:|---:|---|
| RED reproduction, item 1 (permanent block): predecessor single-PID `taskkill` seam + child/grandchild tree | OpenClaw | Linux; CPython 3.10.11 | predecessor fixtures | — | `run_process` returned only at the descendant's natural 60 s exit (`rc=0`); the drain thread stayed blocked on the pipe and `process.stdout.close()` blocked — the exact block class Codex reported | RED CONFIRMED |
| RED reproduction, item 3 (CRLF): Windows-translated bytes `a\r\r\nb\rc\r\n` through product normalization | OpenClaw | Linux; CPython 3.10.11 | predecessor fixture | — | normalized to `a\n\nb\nc\n`, not `a\nb\nc\n` | RED CONFIRMED |
| RED reproduction, item 4 (dangling launcher): predecessor `.cmd` referenced a non-existent `fake_cmake.py` | OpenClaw | Linux (static) | predecessor fixtures | — | referenced script does not exist; launch-failure test deleted the extensionless path, which is not the Windows launcher | RED CONFIRMED |
| RED reproduction, item 5 (non-traversable simulation): predecessor Windows redirect simulation patched no `_listdir`; NTFS cannot hold two case-only files | OpenClaw | Linux (static) | predecessor fixtures | — | logical link entry invisible to the walk; casefold fixture impossible on NTFS | RED CONFIRMED |
| Focused gate: `python -m pytest tools/stm32-toolkit/tests/test_process.py tools/stm32-toolkit/tests/test_build_runner.py tools/stm32-toolkit/tests/test_build_map.py tools/stm32-toolkit/tests/test_firmware_identity.py tools/stm32-toolkit/tests/test_context.py -q` | OpenClaw | Linux; CPython 3.10.11 | `ddd869dc` | 0 | 250 passed; zero failures/errors; no skip/xfail in the five files | PASS |
| `test_process.py` standalone termination: `python -m pytest tools/stm32-toolkit/tests/test_process.py -q` | OpenClaw | same | `ddd869dc` | 0 | 26 passed in 18.1 s; clean exit without any outer force-timeout; no residual `time.sleep(60)`/taskkill/cmake/ninja children afterwards | PASS |
| `python -m pytest tools/stm32-toolkit/tests/test_result.py tools/stm32-toolkit/tests/test_generation.py -q` | OpenClaw | same | `ddd869dc` | 0 | all passed | PASS |
| Full suite: `python -m pytest tools/stm32-toolkit/tests -q` | OpenClaw | same | `ddd869dc` | 0 | 1048 collected, 1031 passed, 17 skipped (the pre-existing accepted-base Windows-only skips, unchanged; no new skip/xfail) | PASS |
| Branch coverage: `python -m pytest tools/stm32-toolkit/tests -q --cov=stm32_toolkit --cov-branch --cov-report=term` (clean `.coverage`) | OpenClaw | same | `ddd869dc` | 0 | **93% TOTAL** (6097 stmts, 408 miss; 1998 branches, 160 miss; fail_under 90 satisfied); build.model 100%, build.map_file 97%, build.identity 95%, build.runner 93%, build.process 91%, build.__init__ 100%, context 95% | PASS |
| Compile: `python -m compileall -q tools/stm32-toolkit/src tools/stm32-toolkit/tests` | OpenClaw | same | `ddd869dc` | 0 | silent | PASS |
| Revision diff hygiene: `git diff --check d130b033…...HEAD` | OpenClaw | same | `ddd869dc` | 0 | silent | PASS |
| Revision diff scope: `git diff --name-status d130b033…...HEAD` | OpenClaw | same | `ddd869dc` | 0 | exactly the §2 revision inventory (three test files) | PASS |
| Full diff hygiene: `git diff --check e47eee0d…...HEAD` | OpenClaw | same | `ddd869dc` | 0 | silent | PASS |
| Working tree: `git status --short` | OpenClaw | same | `ddd869dc` | 0 | empty before the report commit | PASS |
| Windows focused/full, real process-tree timeout, `msvcrt.locking`, replace/fsync publication | Codex | Windows NT 10.0.26200.0; CPython 3.12.13 | returned head | — | real Windows process groups/`taskkill /T`, `msvcrt.locking`, and the full Windows suite must be re-run by Codex on the returned head; the Windows branches are seam-tested on Linux with real child processes, and the seam fixtures are now deterministic and NTFS-independent | `DEFERRED_TO_CODEX` |
| Real CMake 4.3.1 + Ninja 1.13.2 + ARM GNU 14.3.1/binutils 2.44 debug+release builds of the `minimal-gcc` fixture | Codex | Windows; CMake 4.3.1, Ninja 1.13.2, ARM GNU 14.3.1 | returned head | — | configure+build of both presets must exit 0 with valid MAP/ELF/identity; OpenClaw verifies the runner end-to-end with the hit-proven fake toolchain only | `DEFERRED_TO_CODEX` |
| Visual/hardware | N/A | N/A | — | — | `NOT_APPLICABLE` — no UI or hardware access | N/A |

Attribution rules are honored: every `PASS` row above is an OpenClaw run with its own observed exit and result; no seam/static inspection is described as a real Windows run; no Codex observation is claimed as OpenClaw evidence; the two Codex gates are recorded only as `DEFERRED_TO_CODEX` and never as PASS.

## 4. Regression and residual risk

- Regression proof: the focused gate (250 passed) and full suite (1031 passed, 17 pre-existing skips) are green on the code head; branch coverage is 93% overall with every new build/process module ≥90% — identical totals to the reviewed predecessor, so the fixture corrections closed no coverage gap and opened no new one. The revision diff touches only test files; the product, schemas, templates, lockfile, and dependency set are byte-identical to the reviewed predecessor.
- Residual risks (unchanged in nature, now with deterministic fixtures):
  1. Real Windows process-group creation/termination (`taskkill /T`), `msvcrt.locking`, and the full Windows suite remain Codex's gate. The seam fixtures are now deterministic on Windows (whole-tree kill via real `taskkill /T`, `OpenProcess`-based liveness, real `.cmd`+`.py` launchers, `_listdir`/`lstat`/`resolve` redirect simulation) but have not been executed on Windows here.
  2. Real CMake/Ninja/ARM GNU builds of `minimal-gcc` remain Codex's gate; OpenClaw exercises the runner end-to-end only with the hit-proven Python fake.
  3. Defensive branches unreachable through public APIs remain covered by direct unit tests or platform-excluded markers as before.
- Follow-up recommendation: unchanged from the reviewed predecessor (later CLI/MCP modules may consume `stm32_toolkit.build.run_build`).

## 5. Author checklist

- [x] Accepted base and code head are full SHAs (`e47eee0d374bd3a959fe555990b66a6163eb18b8`, `ddd869dccb2eaf0d0f155f06b78d7dc55a3d360d`); reviewed predecessor `d130b033acac407c3416530808431915f1d11f09` recorded.
- [x] Final head is returned out of band after this report commit (PR metadata + return message); this report contains no self-referential final SHA and no moving commit totals.
- [x] Revision diff contains exactly the three allowed test files plus this report; full r002 diff scope from the accepted base is unchanged (23 mandated paths + report).
- [x] Each of the five Codex rejections has a Linux seam RED reproduction and a GREEN fixture correction; no product code, schema, template, dependency, or lockfile changed; no skip/xfail added.
- [x] Every required OpenClaw gate has direct observed evidence on the code head (RED reproductions; focused 250 passed; full 1031 passed / 17 pre-existing skips; branch coverage 93%; compileall; both diff checks; clean status; standalone `test_process.py` termination).
- [x] Other-environment gates are accurately attributed or deferred (Windows suite/process-tree/lock and real ARM toolchain builds `DEFERRED_TO_CODEX`; visual `NOT_APPLICABLE`); no seam/static inspection is described as a real Windows run; no Codex evidence is claimed as OpenClaw evidence.
- [x] No credentials, private data, caches, build output, or temp projects are committed (venv and disposable RED runs under `/tmp`; fake toolchain written at test time; `.coverage`/`__pycache__`/`.pytest_cache` ignored).
- [x] Overall status `IMPLEMENTED`: complete suite exits 0 on CPython 3.10.11 (1031 passed, 17 pre-existing skips), branch coverage 93% with every new build/process module ≥90%, Windows and real-toolchain gates `DEFERRED_TO_CODEX`.
