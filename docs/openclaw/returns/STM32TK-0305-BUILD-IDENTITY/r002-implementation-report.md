# STM32TK-0305-BUILD-IDENTITY r002 Implementation Report — Revision 2

Status: `IMPLEMENTED`
Module: `STM32TK-0305-BUILD-IDENTITY` / r002 / revision-2
Branch: `openclaw/STM32TK-0305-BUILD-IDENTITY/r002`
Accepted base commit: `e47eee0d374bd3a959fe555990b66a6163eb18b8` (exactly matches the work order; working tree clean at start)
Reviewed predecessor: `d9d1a52559f3d9230c4d584bf309a8a4d435ea8e` (the r002 revision-1 report commit)
Code head before this report commit: `986f1f124bbee46812ee7704b7847774db56bb72`
Final branch head: supplied only in the return message and PR metadata
Work order: `docs/openclaw/modules/STM32TK-0305-BUILD-IDENTITY.md` (specification commit `cc2e4947aa692f940655c9ddf18ee8ab1f68c824`, `READY_FOR_OPENCLAW`, accepted base matches)

## 1. Revision verdict and scope

Codex returned `REVISION_REQUIRED` for the reviewed predecessor `d9d1a525…` with two P0 fixture defects (plus an explicit no-regression constraint). Both defects are Windows-host test-fixture defects; no product-code change was required or made in this round. Each fix is confined to exactly one allowed test path and is proven by a Linux reproduction of the Windows failure class followed by the GREEN fixture correction on the code head.

| # | Codex rejection (observed on Windows NT 10.0.26200.0, CPython 3.12.13) | Root cause | Fix in this round | RED → GREEN evidence |
|---|---|---|---|---|
| 1 | `test_timeout_uses_graceful_then_force_termination_seams` raises `AttributeError` on Windows because `signal.SIGKILL` does not exist there; the real child interpreter then runs to its natural 60 s exit | The seam's force-kill body referenced the nonexistent `signal.SIGKILL` in the non-POSIX branch | The force-kill seam now uses `os.kill(pid, signal.SIGTERM)` on non-POSIX hosts, which CPython documents as the unconditional TerminateProcess force-kill; the graceful→force contract (graceful seam hit, force seam hit, child reaped) is asserted against the Popen root PID with the script-interpreter PID kept as a distinct quantity | RED: static — `signal.SIGKILL` is absent from the Windows `signal` module. GREEN: `test_process.py` exits 0 in 18.0 s with the graceful→force test at 3.0 s (bounded poll, no 60 s natural-exit wait) and both PID assertions reaped |
| 2 | `test_windows_branch_uses_taskkill_seam` and `test_windows_branch_graceful_signal_is_seam_injected` fail with `TypeError` on Windows: the fake `taskkill` calls `subprocess.run`, which is routed through the globally replaced `subprocess.Popen` proxy, and the proxy is not a context manager | `subprocess.run` executes `with Popen(...)`, so the proxy lacking `__enter__`/`__exit__` raises `TypeError`; the child tree then survives to natural exit | The fake `taskkill` now launches the real `taskkill` through the genuine `subprocess.Popen` captured at import time (never intercepted), and the Windows-branch Popen proxy additionally implements the context-manager protocol so any residual `subprocess.run` through it cannot raise `TypeError` | RED: stdlib inspection — `subprocess.run` requires a context-manager Popen; predecessor proxy had no `__enter__`/`__exit__`. GREEN: both whole-tree tests exit 0 at 3.0 s each with the taskkill seam hit on the Popen root PID and child + grandchild both reaped |
| 3 | `test_windows_branch_falls_back_to_kill_when_taskkill_fails` conflates the Popen launcher PID with the script-recorded interpreter PID | The seam-hit and reaping assertions used the pid file value as if it were the pid the product actually tracked | Assertions now distinguish the Popen root process (recorded by the proxy instance), the command launcher, and the script interpreter (the pid the child writes); the taskkill seam is asserted to receive the Popen root PID, the `Popen.kill()` fallback is asserted to have run on the root, the direct-exec identity `interpreter == root` is asserted explicitly, and reaping is asserted for each PID independently | RED: single-PID assumption is not verifiable across launcher configurations. GREEN: fallback test exits 0 at 3.0 s with `taskkilled == [root_pid]`, `root.kill_called is True`, and both PIDs reaped |
| 4 | Windows fake CMake never intercepts the product's bare `"cmake"` argv: `test_run_build_success_debug_publishes_exact_evidence` and `test_launch_failure_returns_configure_failed` fail because Windows `CreateProcess` appends `.exe` to a bare name and resolves the ambient real CMake instead of the test's `cmake.cmd` (the launcher-probe test that invokes `cmake.cmd` by full path passes) | PATH-based `.cmd` interception cannot work for a bare `"cmake"` argv on Windows | The test layer now installs a narrow deterministic process-launch seam (`_CmakeOnlyPopenSeam`): only an original argv whose executable is exactly `"cmake"` is mapped to `sys.executable` plus the real `fake_cmake.py` script; the untouched product argv is recorded and asserted to remain exactly `("cmake", "--preset", preset)` / `("cmake", "--build", "--preset", preset[, "--clean-first"])`; every other invocation (Git evidence, helper `subprocess.run`) is delegated unchanged to the real `subprocess.Popen`. The launch-failure test now deletes the seam's actual launch target (`fake_cmake.py`), after which the seam raises `FileNotFoundError` exactly like a real spawn and the product deterministically returns `BUILD_CONFIGURE_FAILED` with `rule=launch` | RED: Codex observed bare `"cmake"` resolve to the ambient real CMake on Windows; stdlib inspection confirms `shell=False` passes the executable straight to `CreateProcess`. GREEN: success fixture exits 0 on Linux with exactly two hit records (configure + build) plus the recorded original argv `["cmake", "--preset", "arm-debug"]` / `["cmake", "--build", "--preset", "arm-debug"]`; launch-failure fixture returns `BUILD_CONFIGURE_FAILED` / `rule=launch` |

During verification, an additional fixture defect of the same class was caught and fixed within the same allowed path: the Windows-branch Popen proxy stripped `start_new_session`, which on POSIX broke the graceful→force test's process-group kill and reintroduced the exact forbidden 60 s natural-exit wait (Linux repro: `test_timeout_uses_graceful_then_force_termination_seams` at 60.09 s). The proxy now strips only the Windows-only `creationflags` kwarg and leaves `start_new_session` untouched (POSIX uses it to create the child's own group; Windows `Popen` silently ignores it). After the fix the slowest `test_process.py` test is 3.01 s and the file completes in 18.0 s with no residual processes.

No product file, schema, template, dependency, or lockfile was modified; `tools/stm32-toolkit/tests/test_firmware_identity.py` was not touched, so all §5.3 behaviors (escaping redirect rejection, casefold collision rejection, in-root symlinked header acceptance, redirect loop rejection, CRLF raw-disk-byte digest) are byte-identical to the reviewed predecessor and remain green on the code head (the five-file gate below includes them). No skip/xfail was added anywhere; the 17 full-suite skips are the pre-existing accepted-base Windows-only skips, unchanged.

## 2. Changed-path inventory (revision diff `d9d1a525…` → code head `986f1f12…`)

| Status | Path | Purpose |
|---|---|---|
| M | `tools/stm32-toolkit/tests/test_process.py` | Windows-safe force-kill seam (no `signal.SIGKILL`), real-Popen-driven `taskkill` fake, context-manager-complete Popen proxy that strips only `creationflags`, PID-distinguished assertions (Popen root / launcher / interpreter), preserved `start_new_session` for the POSIX group-kill contract |
| M | `tools/stm32-toolkit/tests/test_build_runner.py` | narrow `_CmakeOnlyPopenSeam` mapping only exact bare `"cmake"` argv to `sys.executable` + real `fake_cmake.py`; recorded-original-argv assertions; launch-failure fixture destroys the seam's real target; launcher files kept for the full-path probe contract |
| A | `docs/openclaw/returns/STM32TK-0305-BUILD-IDENTITY/r002-implementation-report.md` | this report (report-only final commit) |

`git diff --check d9d1a525…...HEAD` and `git diff --name-status d9d1a525…...HEAD` confirm exactly these paths and no whitespace errors. The full r002 diff from the accepted base (`e47eee0d…` → HEAD) contains the mandated implementation paths plus this report, with `git diff --check e47eee0d…...HEAD` silent. No commit was amended, rebased, cherry-picked, or force-pushed; `master`, the specification commit, the r001 branch, and PR #5 were not touched.

## 3. Environment-separated verification (this revision round)

OpenClaw environment: Linux x86_64 (`Linux 7.0.0-22-generic`); CPython 3.10.11 (`/home/openclaw/coding/venvs/tk0302`, outside the repository); jsonschema 4.23.0, mcp 1.27.0, pyelftools 0.33, Jinja2 3.1.6, pytest 8.3.5, pytest-cov 6.0.0. The venv's editable install metadata points at an older mirror path, so all commands ran with `PYTHONPATH` set to the checked-out branch tree (`tools/stm32-toolkit/src`), verified by `stm32_toolkit.__file__` resolving inside the working tree. All commands ran from the repository root on branch `openclaw/STM32TK-0305-BUILD-IDENTITY/r002` at code head `986f1f12…`; the report commit follows separately.

| Gate/command | Owner | Environment | Commit tested | Exit | Observed result | Status |
|---|---:|---:|---:|---|
| RED reproduction, item 2 (proxy is not a context manager): stdlib inspection of `subprocess.run` (`with Popen(...)`) against the predecessor proxy | OpenClaw | Linux; CPython 3.10.11 | predecessor fixtures | — | `subprocess.run` requires `__enter__`/`__exit__`; predecessor proxy had neither — the exact `TypeError` class Codex observed | RED CONFIRMED |
| RED reproduction, item 1/4 (static): `signal.SIGKILL` absent from Windows `signal`; bare `"cmake"` → CreateProcess appends `.exe`, so `cmake.cmd` in PATH can never intercept | OpenClaw | Linux (static, stdlib/CPython source) | predecessor fixtures | — | both failure mechanisms confirmed from CPython behavior | RED CONFIRMED |
| RED reproduction, proxy regression (caught during verification): predecessor proxy stripped `start_new_session` → graceful→force child not a group leader → `killpg` missed → 60.09 s natural-exit wait | OpenClaw | Linux; CPython 3.10.11 | intermediate fixture | — | `test_timeout_uses_graceful_then_force_termination_seams` at 60.09 s — the exact forbidden wait class | RED CONFIRMED |
| `python -m pytest tools/stm32-toolkit/tests/test_process.py -q` | OpenClaw | Linux; CPython 3.10.11 | `986f1f12` | 0 | 26 passed in 18.0 s; slowest test 3.01 s (bounded polls, no 60 s wait); `pgrep -af "time.sleep(60)"` before and after the run found no residual test processes | PASS |
| `python -m pytest tools/stm32-toolkit/tests/test_build_runner.py -q -k "fake_cmake_launcher_reaches_the_python_double or run_build_success_debug_publishes_exact_evidence or launch_failure_returns_configure_failed"` | OpenClaw | same | `986f1f12` | 0 | 3 passed (launcher probe, exact-evidence success with recorded original argv, launch failure) | PASS |
| Focused gate: `python -m pytest tools/stm32-toolkit/tests/test_process.py tools/stm32-toolkit/tests/test_build_runner.py tools/stm32-toolkit/tests/test_build_map.py tools/stm32-toolkit/tests/test_firmware_identity.py tools/stm32-toolkit/tests/test_context.py -q` | OpenClaw | same | `986f1f12` | 0 | 250 passed; 0 skipped, 0 failed (junitxml: tests=250, errors=0, failures=0, skipped=0) | PASS |
| Full suite: `python -m pytest tools/stm32-toolkit/tests -q` | OpenClaw | same | `986f1f12` | 0 | 1048 collected; 1031 passed, 17 skipped (the pre-existing accepted-base Windows-only skips; no new skip/xfail), 0 failures/errors (junitxml: tests=1048, errors=0, failures=0, skipped=17) | PASS |
| Branch coverage: `python -m pytest tools/stm32-toolkit/tests -q --cov=stm32_toolkit --cov-branch --cov-report=term` (clean `.coverage`) | OpenClaw | same | `986f1f12` | 0 | **93% TOTAL** (6097 stmts, 408 miss; 1998 branches, 160 miss; fail_under 90 satisfied); build.model 100%, build.map_file 97%, build.identity 95%, build.runner 93%, process 91%, build.__init__ 100%, context 95% | PASS |
| Compile: `python -m compileall -q tools/stm32-toolkit/src tools/stm32-toolkit/tests` | OpenClaw | same | `986f1f12` | 0 | silent | PASS |
| Revision diff hygiene: `git diff --check d9d1a525…...HEAD` | OpenClaw | same | `986f1f12` | 0 | silent | PASS |
| Revision diff scope: `git diff --name-status d9d1a525…...HEAD` | OpenClaw | same | `986f1f12` | 0 | exactly the §2 revision inventory (two test files) | PASS |
| Working tree: `git status --short` | OpenClaw | same | `986f1f12` | 0 | empty before the report commit | PASS |
| Windows focused/full, real process-tree timeout, `msvcrt.locking`, replace/fsync publication | Codex | Windows NT 10.0.26200.0; CPython 3.12.13 | returned head | — | real Windows process groups/`taskkill /T`, `msvcrt.locking`, and the full Windows suite must be re-run by Codex on the returned head; the Windows branches are seam-tested on Linux with real child processes, and the fixtures no longer reference `signal.SIGKILL`, no longer route the fake `taskkill` through the Popen proxy, and no longer rely on bare-name `.cmd` resolution | `DEFERRED_TO_CODEX` |
| Real CMake 4.3.1 + Ninja 1.13.2 + ARM GNU 14.3.1/binutils 2.44 debug+release builds of the `minimal-gcc` fixture | Codex | Windows; CMake 4.3.1, Ninja 1.13.2, ARM GNU 14.3.1 | returned head | — | configure+build of both presets must exit 0 with valid MAP/ELF/identity; OpenClaw verifies the runner end-to-end with the hit-proven fake toolchain only | `DEFERRED_TO_CODEX` |
| Visual/hardware | N/A | N/A | — | — | `NOT_APPLICABLE` — no UI or hardware access | N/A |

Attribution rules are honored: every `PASS` row above is an OpenClaw run with its own observed exit and result; RED rows are Linux/static reproductions of the Windows failure class and are labeled as such; no seam/static inspection is described as a real Windows run; no Codex observation is claimed as OpenClaw evidence; the two Codex gates are recorded only as `DEFERRED_TO_CODEX` and never as PASS.

## 4. Regression and residual risk

- Regression proof: the five-file gate (250 passed, 0 skipped) and full suite (1048 collected; 1031 passed, 17 pre-existing skips, 0 failures/errors) are green on the code head; branch coverage is 93% overall with every new build/process module ≥90% (process 91%, runner 93%, identity 95%, map_file 97%, model 100%). The revision diff touches only the two allowed test files; the product, schemas, templates, lockfile, and dependency set are byte-identical to the reviewed predecessor. `test_firmware_identity.py` is untouched, so all §5.3 behaviors remain byte-identical and green.
- Residual risks (unchanged in nature, now with deterministic fixtures):
  1. Real Windows process-group creation/termination (`taskkill /T`), `msvcrt.locking`, and the full Windows suite remain Codex's gate. The Windows fixtures are now deterministic by construction (no `signal.SIGKILL`, fake `taskkill` drives the real `subprocess.Popen`, narrow `"cmake"`-only launch seam, `OpenProcess`-based liveness) but have not been executed on Windows here.
  2. Real CMake/Ninja/ARM GNU builds of `minimal-gcc` remain Codex's gate; OpenClaw exercises the runner end-to-end only with the hit-proven Python double.
  3. Git-evidence tests install a fake `git` via PATH; on Windows a bare `"git"` resolves to the ambient real `git.exe` (CreateProcess appends `.exe`), the same pre-existing limitation as the old cmake PATH launcher. Per the work order the launch seam must not intercept Git, so the fake-git Windows behavior is unchanged from the reviewed predecessor and falls under the deferred Windows full gate.
- Follow-up recommendation: unchanged from the reviewed predecessor (later CLI/MCP modules may consume `stm32_toolkit.build.run_build`).

## 5. Author checklist

- [x] Accepted base and code head are full SHAs (`e47eee0d374bd3a959fe555990b66a6163eb18b8`, `986f1f124bbee46812ee7704b7847774db56bb72`); reviewed predecessor `d9d1a52559f3d9230c4d584bf309a8a4d435ea8e` recorded.
- [x] Final head is returned out of band after this report commit (PR metadata + return message); this report contains no self-referential final SHA and no moving commit totals.
- [x] Revision diff contains exactly the two allowed test files plus this report; no product code, schema, template, dependency, or lockfile changed; `test_firmware_identity.py` untouched; no skip/xfail added.
- [x] Each Codex rejection has a RED reproduction of the failure class and a GREEN fixture correction on the code head; the proxy `start_new_session` regression was caught and fixed within the same allowed path; timeout tests use bounded polls only and leave no residual processes (verified by `pgrep` before/after).
- [x] Every required OpenClaw gate has direct observed evidence on the code head (RED reproductions; `test_process.py` 26 passed in 18.0 s with slowest 3.01 s; focused 3 passed; five-file 250 passed / 0 skipped; full 1048 collected / 1031 passed / 17 pre-existing skips / 0 failures; branch coverage 93% with every new build/process module ≥90%; compileall; both diff checks; clean status).
- [x] Other-environment gates are accurately attributed or deferred (Windows suite/process-tree/lock and real ARM toolchain builds `DEFERRED_TO_CODEX`; visual `NOT_APPLICABLE`); no seam/static inspection is described as a real Windows run; no Codex evidence is claimed as OpenClaw evidence.
- [x] No credentials, private data, caches, build output, or temp projects are committed (venv outside the repository; disposable RED runs under `/tmp`; fake toolchain written at test time; `.coverage`/`__pycache__`/`.pytest_cache` ignored).
- [x] Overall status `IMPLEMENTED`: complete suite exits 0 on CPython 3.10.11 (1031 passed, 17 pre-existing skips), branch coverage 93% with every new build/process module ≥90%, Windows and real-toolchain gates `DEFERRED_TO_CODEX`.
