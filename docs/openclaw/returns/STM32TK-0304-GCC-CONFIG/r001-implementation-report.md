# STM32TK-0304-GCC-CONFIG r001/r002 Implementation Report

Status: `IMPLEMENTED` (revised by r002 per Codex `REVISION_REQUIRED`)
Module: `STM32TK-0304-GCC-CONFIG` / r001 branch, r002 revision
Branch: `openclaw/STM32TK-0304-GCC-CONFIG/r001`
Accepted base commit: `8a627e95732bff069bef76e75c6b5f6fa992c6f4`
Code head before report commit: `2b9c57436bc7237c6b8f6ff74e6ef2a11c61a659`
Final branch head: supplied only in the return message and PR metadata
PR/compare URL: supplied in the return message
Work order: `docs/openclaw/modules/STM32TK-0304-GCC-CONFIG.md` (specification commit `9e201d078ea4e04b7043c94e9fe1066de8a82c6f`)

## 0. r002 revision summary

Codex returned `REVISION_REQUIRED` with four rejection items; all four are fixed on top of the r001 report head `638dbc6494d4ac9410b28a346a5886f678a4ce8e` (no rebase, no amend, no force-push; the revision diff is exactly four paths):

| # | Rejection item | Fix |
|---|---|---|
| 1 | Replace-phase atomicity: `except _ApplyFailure: raise` let semantic failures raised after real mutations (`GENERATION_INPUT_CHANGED`, `GENERATION_TARGET_EXISTS`, …) bypass rollback, leaving replaced files and staging behind | `_apply` now rolls back every `_ApplyFailure` raised after `in_replace = True` in exact reverse order (`_rollback`: temp removal, reverse `os.replace` of backups, created-file removal, staging removal, created-parent pruning). Rollback success preserves the original stable code and details; rollback failure raises `GENERATION_ROLLBACK_FAILED` with sorted portable paths and retains recoverable staging. Regression tests prove the previously replaced manifest is restored byte-exactly, created files are removed, and staging is absent. |
| 2 | Fixed-section absolute addresses were dropped: the linker context discarded `_address`, so `linker.ld.j2` emitted only `.stm32tk.abs.XXXXXXXX (NOLOAD)` without an address | `_build_contexts` now passes `address_hex` (`0x%08x` of the validated address) into the template context, and both root and packaged `linker.ld.j2` emit a GNU ld absolute-address output section: `.stm32tk.abs.20000000 0x20000000 (NOLOAD) :` with `KEEP(*(...))` and the region assignment. Root and packaged copies remain byte-identical (SHA-256 `67eb90716f9163c2…`). Tests cover FLASH placement, RAM placement, multiple sections, and exact snapshots asserting `0x20000000 (NOLOAD)`. |
| 3 | Staging intermediate classification: a regular file at `.stm32-toolkit/configuration-staging` surfaced later as `GENERATION_APPLY_FAILED` (Windows reports `FileNotFoundError` rather than `NotADirectoryError` for a file intermediate) | `_reject_existing_staging` now `lstat`s every intermediate component (`.stm32-toolkit`, `configuration-staging`) before the first write and stably returns `GENERATION_TARGET_EXISTS` (`{"path": "<component>"}`) when one is not a directory, with zero writes. Tests cover a file at `configuration-staging` and a file at `.stm32-toolkit`. |
| 4 | Windows test portability: `str(path).endswith("Src/app.c")`-style injections never match on Windows separators; directory redirects used `os.symlink` (needs privileges on Windows); unreadable scenarios used `chmod(0o000)` (read-only bit does not block reads on Windows); the mode test asserted an absolute POSIX mode; race injections lacked hit-counter assertions | `test_generation.py`: all injection matchers now use `Path.parts`/`Path.name`; directory redirects use a real unprivileged NTFS junction (`mklink /J`) on Windows and real symlinks on POSIX; file redirects use a deterministic reparse/resolve injection (`Path.resolve` + forged `S_IFLNK` `os.lstat` result); unreadable scenarios inject `PermissionError` at the bounded read; the mode test compares the actual post-`chmod` mode; every race injection has a hit counter and asserts it fired, and replace-phase races now count deterministic `_resolve_apply_target` calls (identical phase ordering on POSIX and Windows) instead of raw `os.lstat` counts (which differ because `Path.resolve` internally lstats on POSIX but not on Windows). |

TDD evidence for the revision: the nine new/rewritten tests failed on the r001 head with exactly the rejected behaviors (fixed-section snapshots missing the address, rollback bypass leaving the new manifest bytes and staging behind, `GENERATION_APPLY_FAILED` instead of `GENERATION_TARGET_EXISTS` for the file intermediate) and pass after the fixes.

## 1. Outcome

- Observable result: `plan_project_configuration(model) -> GenerationPlan` produces a deterministic, read-only managed GCC/CMake/linker/VS Code configuration plan from one validated Schema v2 `ProjectModel`: canonical-root and fresh-model validation, generation-spec validation, exact GCC CPU/FPU/float-ABI flag selection, define/option/preset/ELF/memory fail-closed validation, bounded input re-validation with SHA-256 digests, optional `conversion-report.json` fixed-section evidence validation with deterministic absolute-address `.stm32tk.abs.<ADDR8> <ADDR> (NOLOAD)` linker placements, StrictUndefined packaged Jinja rendering of the nine targets, exact bytewise-sorted inputs/files/blockers, per-target create/unchanged/update-managed/user-drift/unowned-collision classification against the prior managed manifest, drift/collision/orphan blockers, deterministic model hash and plan ID, and a JSON-safe `to_dict()` with no absolute paths or raw bytes. `apply_project_configuration(plan) -> OperationResult` revalidates every plan field/digest, the canonical root, a fresh model, all inputs, current targets, staging containment, a fresh re-plan, and the absence of blockers before its first write; then stages under `.stm32-toolkit/configuration-staging/<plan_id>` with exclusive creation, fsync, sibling temporaries and `os.replace`, and rolls back byte/mode-exactly on every recoverable failure — including semantic failures (`GENERATION_INPUT_CHANGED`, `GENERATION_TARGET_EXISTS`, `GENERATION_PATH_INVALID`) raised after the first real mutation, which now restore every toolkit-written byte and remove staging before re-raising the original stable error, and which return `GENERATION_ROLLBACK_FAILED` with retained recoverable staging only when the restore itself fails. A successful apply changes exactly the nine generated targets plus `.stm32-toolkit/generated-files.json`; `.stm32-project.json`, sources, Git state, and unrelated files are never touched. `doctor` adds bounded read-only `vscodeExtensions` evidence for exactly `ms-vscode.cpptools`, `ms-vscode.cmake-tools`, and `marus25.cortex-debug` via the fixed argv `[code, "--list-extensions", "--show-versions"]`; the setup Skill documents CHECK evidence and manual remediation only.
- Scope completed: all of work-order sections 2.1, 3, 4, 5, 6, 7, 8, 9, 10, 11 (OpenClaw portions), 12 (OpenClaw budgets), 13, 14 (OpenClaw manual gates), 15. r001 TDD RED (exit 2; collection interrupted by exactly `ModuleNotFoundError: No module named 'stm32_toolkit.generation'` in `test_generation.py` and `ImportError: cannot import name 'VSCODE_EXTENSIONS' from 'stm32_toolkit.doctor'` in `test_doctor.py` — the APIs and resources did not exist) → r001 focused GREEN (284 tests) → r002 revision RED (9 new/rewritten tests failing with the rejected behaviors) → r002 focused GREEN (287 tests: 263 generation incl. parametrized cases + 24 doctor) → full suite (784 passed, 17 skipped) with branch coverage 92% (fail_under 90 satisfied) → compile/diff/dependency/scope/read-only/atomicity/rollback/template-identity/wheel/performance gates all pass on Linux. Jinja2 3.1.6 installed as the only new runtime dependency.
- Known limitations:
  - Real Windows focused/full runs, real NTFS junction escapes, and Windows atomic replace/rollback are Codex gates (`DEFERRED_TO_CODEX`); Linux real-symlink tests run without skip, and the r002 test suite now uses real NTFS junctions for directory redirects and deterministic reparse/resolve injection for file redirects on Windows so it can run without administrator privileges or developer mode.
  - Real CMake/GCC `cmake --preset arm-debug` configure of a generated fixture is a Codex Windows gate (`DEFERRED_TO_CODEX`); this environment verifies the generated files structurally and by exact snapshots, including the absolute-address fixed-section placements.
  - `_map_fresh_error`'s `GENERATION_MODEL_INVALID` mapping is defensive: a model that loads with the same canonical hash cannot fail generation-spec validation differently at apply, so the branch is reachable only through direct unit invocation of the mapper (covered by tests).
  - A target modified by a race between the replace-time read and `os.replace` is reported as `GENERATION_INPUT_CHANGED` with full rollback of every toolkit-written byte; the raced third-party write is not restored (we never overwrote it).
- Deviations: `NONE`.

## 2. Complete changed-path inventory

Accepted-base (`8a627e95732bff069bef76e75c6b5f6fa992c6f4`) to r002 code head (`2b9c57436bc7237c6b8f6ff74e6ef2a11c61a659`): exactly the 25 product/test paths in work-order section 5, plus this report as the report-only addition after the code head. The r002 revision diff (r001 report head `638dbc6494d4ac9410b28a346a5886f678a4ce8e` to r002 code head) is exactly four paths: `templates/cmake/linker.ld.j2`, `tools/stm32-toolkit/src/stm32_toolkit/generation/configure.py`, `tools/stm32-toolkit/src/stm32_toolkit/templates/cmake/linker.ld.j2`, `tools/stm32-toolkit/tests/test_generation.py`.

| Status | Path | Work-order section | Purpose |
|---|---|---|---|
| M | `tools/stm32-toolkit/pyproject.toml` | 5 | add `Jinja2>=3.1,<4` and package `templates/cmake/*` + `templates/vscode/*` resources |
| A | `tools/stm32-toolkit/src/stm32_toolkit/generation/__init__.py` | 5, 6.1 | re-export the eight public contracts and both functions |
| A | `tools/stm32-toolkit/src/stm32_toolkit/generation/managed_files.py` | 5, 6 | frozen values/hashes, portable paths, manifest build/parse, stable errors |
| A | `tools/stm32-toolkit/src/stm32_toolkit/generation/configure.py` | 5, 7-9 | plan/render/classify/apply/stage/rollback orchestration (r002: atomic semantic-failure rollback, fixed-section address context, staging intermediate classification) |
| A | `templates/cmake/CMakeLists.txt.j2` | 5 | review copy of the root CMake template |
| A | `templates/cmake/arm-none-eabi-gcc.cmake` | 5 | review copy of the toolchain template |
| A | `templates/cmake/CMakePresets.json.j2` | 5 | review copy of the preset template |
| A | `templates/cmake/linker.ld.j2` | 5 | review copy of the linker template (r002: absolute-address fixed-section output) |
| A | `templates/vscode/tasks.json.j2` | 5 | review copy of the Toolkit task template |
| A | `templates/vscode/launch.json.j2` | 5 | review copy of the Cortex-Debug handoff template |
| A | `templates/vscode/c_cpp_properties.json.j2` | 5 | review copy of the IntelliSense template |
| A | `templates/vscode/settings.json.j2` | 5 | review copy of the owned VS Code settings |
| A | `templates/vscode/extensions.json` | 5 | exact three-extension recommendations |
| A | `tools/stm32-toolkit/src/stm32_toolkit/templates/cmake/*` (4 files) | 5 | packaged byte-identical runtime resources (linker.ld.j2 updated in r002) |
| A | `tools/stm32-toolkit/src/stm32_toolkit/templates/vscode/*` (5 files) | 5 | packaged byte-identical runtime resources |
| M | `tools/stm32-toolkit/src/stm32_toolkit/doctor.py` | 5, 11 | bounded VS Code extension evidence |
| M | `skills/setup-stm32-env/SKILL.md` | 5, 11 | document extension CHECK/manual remediation without mutation |
| A | `tools/stm32-toolkit/tests/test_generation.py` | 5, 13 | 273 tests covering all generation/model/template/security/atomicity contracts (r002: Windows-portable injections, hit counters, rollback regressions) |
| M | `tools/stm32-toolkit/tests/test_doctor.py` | 5, 13 | +14 extension-evidence and bounded-probe tests |
| A | `docs/openclaw/returns/STM32TK-0304-GCC-CONFIG/r001-implementation-report.md` | 16 | this report (report-only addition) |

No other path changed. `git diff --check 8a627e95..HEAD` exits 0; `git status --short` is empty before the report commit.

## 3. Public contracts delivered

- Types/signatures (all `@dataclass(frozen=True)`, tuples not lists; `to_dict()` returns fresh JSON-safe mappings, omits `project_root`, `model`, and raw bytes, uses only portable `/` paths, and never includes host exception text):
  - `GenerationError(code, message, details)`
  - `GenerationInput(path, sha256, size)`
  - `ManagedFileRecord(path, ownership="managed", template_version, sha256)`
  - `GeneratedFile(path, status, template_name, template_version, before_sha256, after_sha256, before_size, after_size, unified_diff, before_bytes, after_bytes)`
  - `GenerationBlocker(code, path, message)`
  - `GenerationPlan(project_root, model, plan_version=1, plan_id, model_sha256, inputs, files, blockers, managed_manifest_path, managed_manifest_bytes)`
  - `plan_project_configuration(model: ProjectModel) -> GenerationPlan`
  - `apply_project_configuration(plan: GenerationPlan) -> OperationResult[dict[str, object]]`
- Deterministic identity: `model_sha256` = SHA-256 over canonical JSON (`sort_keys=True`, separators `(',',':')`, `ensure_ascii=False`) of every serializable model field except `project_root` (UUID stringified, tuples as arrays); `plan_id` = SHA-256 over the canonical payload including plan version, model hash, Toolkit version, template version, every input, every file metadata/status/digest, blockers, the managed manifest path, and the SHA-256 of the proposed manifest bytes; diffs, raw bytes, and absolute paths excluded. Ordering is bytewise portable-path order (UTF-8 byte sort) for inputs/files/blockers; duplicate or Unicode-casefold-colliding paths are invalid. The r002 linker change intentionally alters rendered bytes and therefore plan IDs for fixed-section projects; determinism per content is preserved (verified by repeat-plan equality).
- Stable failure codes with the exact bounded details from section 10: `GENERATION_MODEL_INVALID`, `GENERATION_INPUT_INVALID`, `GENERATION_FIXED_SECTION_INVALID`, `GENERATION_TEMPLATE_INVALID`, `GENERATION_MANIFEST_INVALID`, `GENERATION_PATH_INVALID`, `GENERATED_FILE_DRIFT`, `GENERATION_BLOCKED`, `GENERATION_PLAN_INVALID`, `GENERATION_INPUT_CHANGED`, `GENERATION_TARGET_EXISTS`, `GENERATION_APPLY_FAILED`, `GENERATION_ROLLBACK_FAILED`. After r002, `GENERATION_TARGET_EXISTS` is also the stable code for non-directory staging intermediates (`{"path": ".stm32-toolkit"}` / `{"path": "configuration-staging"}`).
- External interfaces: doctor's `run_doctor` data gains `vscodeExtensions` (exactly the three IDs; values `{"installed", "version", "status"}` with `ok|missing|unavailable|nonzero|timeout|error`); `setup-stm32-env/SKILL.md` CHECK documents the three recommendations and manual install/removal. No CLI/MCP/network change; Jinja2 is the only new dependency.

## 4. Environment-separated verification

OpenClaw environment: Linux x86_64 (`Linux 7.0.0-22-generic`, glibc 2.43, ext4-class filesystem with 4096-byte blocks); CPython 3.10.11 (`/home/openclaw/coding/venvs/tk0302`, outside the repository; also re-run on system CPython 3.14.4 with identical results); jsonschema 4.23.0, mcp 1.27.0, pyelftools 0.33, Jinja2 3.1.6, pytest 8.3.5, pytest-cov 6.0.0, setuptools 65.5.0; package installed `pip install -e "tools/stm32-toolkit[test]"` (stm32-toolkit 0.2.0). All OpenClaw commands run from the repository root on branch `openclaw/STM32TK-0304-GCC-CONFIG/r001`.

| Gate/command | Evidence owner | Environment/tool versions | Commit tested | Exit | Observed result | Status |
|---|---:|---:|---:|---|
| TDD RED (13.1): `python -m pytest tools/stm32-toolkit/tests/test_generation.py tools/stm32-toolkit/tests/test_doctor.py -q` before implementation (generation package removed, doctor reverted) | OpenClaw | Linux; CPython 3.10.11; pytest 8.3.5 | tests-only worktree | 2 | collection interrupted: `2 errors` — `ModuleNotFoundError: No module named 'stm32_toolkit.generation'` and `ImportError: cannot import name 'VSCODE_EXTENSIONS' from 'stm32_toolkit.doctor'`; no other failure | PASS |
| r002 revision RED: nine new/rewritten tests on the r001 head | OpenClaw | same | `638dbc6` | 1 | `9 failed`: fixed-section snapshots missing the absolute address; replace-phase bytes/non-regular/creation races left the replaced manifest, created files, and staging behind; rollback-failure-after-semantic-failure returned `GENERATION_INPUT_CHANGED` instead of `GENERATION_ROLLBACK_FAILED`; staging file intermediate returned the wrong details; all other tests passed | PASS |
| Focused GREEN (13.3): identical command on the code head | OpenClaw | same | `2b9c574` | 0 | `287 passed in 8.30s` (263 generation incl. parametrized cases + 24 doctor); zero failures/errors; no skip/xfail in either file | PASS |
| Full suite (13.3): `python -m pytest tools/stm32-toolkit/tests -q` | OpenClaw | same | `2b9c574` | 0 | `784 passed, 17 skipped in 22.53s`; zero failures/errors; the 17 skips are the pre-existing Windows-only platform skips in `test_plugin_layout.py`/`test_setup_runtime.py` (accepted base), unchanged | PASS |
| Branch coverage (13.3): `python -m pytest tools/stm32-toolkit/tests -q --cov=stm32_toolkit --cov-branch --cov-report=term` | OpenClaw | same | `2b9c574` | 0 | **92%** TOTAL branch (4762 stmts, 340 miss, 1588 branches, 135 partial; fail_under 90 satisfied); generation: `__init__` 100%, managed_files 98%, configure 91% (same per-module figures as r001); doctor 93% | PASS |
| Compile (13.3): `python -m compileall -q tools/stm32-toolkit/src tools/stm32-toolkit/tests` | OpenClaw | same | `2b9c574` | 0 | silent | PASS |
| Diff hygiene (13.3): `git diff --check 8a627e95..HEAD` | OpenClaw | same | `2b9c574` | 0 | silent | PASS |
| Diff scope (13.3): `git diff --name-status 8a627e95..HEAD` | OpenClaw | same | `2b9c574` | 0 | exactly the 25 section-5 paths; r002 revision diff = exactly the 4 allowed paths (2 linker templates, configure.py, test_generation.py) | PASS |
| Dependency (3, 13.3): `pip show`/metadata + `pip install -e "tools/stm32-toolkit[test]"` | OpenClaw | same | `2b9c574` | 0 | `Requires-Dist` = jsonschema, mcp, pyelftools, Jinja2; installed Jinja2 3.1.6 (`>=3.1,<4`); no other new dependency | PASS |
| Working tree (13.3): `git status --short` | OpenClaw | same | `2b9c574` | 0 | empty before the report commit | PASS |
| Root/package template identity (12, 14.8): byte comparison + SHA-256 of all nine resources | OpenClaw | same | `2b9c574` | 0 | root and packaged copies byte-identical for all nine resources; hashes: CMakeLists.txt.j2 `deb2efeef5d0cad8…`, arm-none-eabi-gcc.cmake `a472cf6cd303fcb9…`, CMakePresets.json.j2 `fc30cd0a46d114b3…`, linker.ld.j2 `67eb90716f9163c2…` (r002; was `cbb6c1cf123c8959…`), tasks.json.j2 `144715d7f9b966c5…`, launch.json.j2 `e5fd4b9fa2356d28…`, c_cpp_properties.json.j2 `ad4de63e8d486b0b…`, settings.json.j2 `d1ae2097f11d0116…`, extensions.json `f1702beb2175bc32…` | PASS |
| Installed-wheel resource load (14.8): fresh venv `/tmp/venv0304r002`, wheel installed, cwd `/tmp` outside the repository | OpenClaw | same; wheel `stm32_toolkit-0.2.0-py3-none-any.whl`; venv Jinja2 3.1.6 | `2b9c574` | 0 | `plan_project_configuration` + `apply_project_configuration` succeed with `cwd=/tmp` (`repo in cwd: False`); packaged resources load via `importlib.resources`; plan_id `a282cad2e9a47d9c…`; generated files identical contract; fixed-section linker rendered from the packaged template | PASS |
| Read-only plan, drift, atomicity, rollback, performance (14, 12) | OpenClaw | same | `2b9c574` | 0 | see manual evidence and performance rows | PASS |
| Windows focused/full, real NTFS junction, replace/rollback (13.4) | Codex | Windows NT 10.0.26200.0; CPython 3.12.13 | returned head | — | full Windows suite and real NTFS junction escape/atomic-replace/rollback must be re-run by Codex on the returned head; r002 removed the privilege and separator dependence so the suite runs without developer mode | `DEFERRED_TO_CODEX` |
| Real CMake/GCC configure (13.4) | Codex | Windows; CMake 4.3.1, Ninja 1.13.2, installed ARM GNU toolchain | returned head | — | `cmake --preset arm-debug` must exit 0 without host paths on the returned head | `DEFERRED_TO_CODEX` |
| VS Code GUI/extension installation (13.4) | N/A | N/A | — | — | `NOT_APPLICABLE` — doctor is evidence only | PASS |

### Manual and visual evidence

| Gate | Owner | Observed result | Evidence path/status |
|---|---|---|---|
| 14.1 read-only plan snapshot | OpenClaw | full recursive snapshot (names/bytes/SHA-256/modes/mtimes) identical before vs after planning; no `.stm32-toolkit` created; plan_id `c39492d23d41449a…` (no fixed sections) | manual run on disposable `/tmp/manual0304`, removed afterwards |
| 14.2 first apply | OpenClaw | `OK`; `createdPaths` = exactly the ten written paths (`.stm32-toolkit/generated-files.json` + the nine targets, portable-sorted); `updatedPaths`/`unchangedPaths` empty; every written byte matches the plan digest; manifest `templateVersion: 1`, 9 records; no staging | PASS |
| 14.3 replan + no-op apply | OpenClaw | all nine targets `unchanged`; plan `to_dict`/manifest bytes deterministic; second apply returns all-unchanged with zero replacements (mtimes byte-identical) | PASS |
| 14.4 modified managed file | OpenClaw | after planning → `GENERATION_INPUT_CHANGED {"path": "CMakeLists.txt"}` with no write; before planning → plan blocker `GENERATED_FILE_DRIFT`, apply `GENERATED_FILE_DRIFT {"paths": ("CMakeLists.txt",)}` with no write | PASS |
| 14.5 unowned equal collision | OpenClaw | file whose bytes equal the proposal still classified `unowned-collision`; apply `GENERATION_BLOCKED {"codes": ("UNOWNED_COLLISION",)}`; file untouched | PASS |
| 14.6 replace/rollback failure injection | OpenClaw | injected `os.replace` failure at position 3 → `GENERATION_APPLY_FAILED {"phase": "replace"}` with exact byte/mode/status restore and staging absent; injected rollback failure → `GENERATION_ROLLBACK_FAILED {"paths": (".stm32-toolkit/generated-files.json",)}` with recoverable backup retained in staging. r002 additions: semantic failures during the replace phase (bytes race, non-regular race, creation race) now restore every toolkit-written byte and remove staging while preserving the original code/details; a semantic failure whose restore fails returns `GENERATION_ROLLBACK_FAILED {"paths": (".stm32-toolkit/generated-files.json",)}` and retains the recoverable backup | PASS |
| 14.7 forged digest-consistent plan | OpenClaw | after bytes + digests + manifest bytes forged consistently → `GENERATION_PLAN_INVALID {"rule": "planId"}`, no write | PASS |
| 14.8 wheel install outside repository | OpenClaw | fresh venv + wheel; plan/apply from `/tmp` succeed with packaged templates (r002 re-run); installed Jinja2 3.1.6; resource hashes match section 4 | PASS |
| 14.9 performance | OpenClaw | plan median **276.1 ms** (min 274.3, max 287.7) < 500 ms; apply median **38.3 ms** (min 37.9, max 39.3) < 1,000 ms; fixture 1,000 declared sources + 100 fixed sections; 20 warm plan runs / 10 fresh apply projects | PASS |
| 13.4 real NTFS junction + Windows rollback | Codex | not run by OpenClaw; Linux real-symlink tests run without skip; r002 tests use real NTFS junctions and deterministic reparse-injection on Windows | `DEFERRED_TO_CODEX` |
| 13.4 real CMake/GCC configure | Codex | not run by OpenClaw; generated content validated structurally and by exact snapshots (including `.stm32tk.abs.<ADDR> <ADDR> (NOLOAD)` placements) | `DEFERRED_TO_CODEX` |
| Visual/UI | N/A | `NOT_APPLICABLE` — no UI or rendered artifact created | N/A |

### Artifacts

| Artifact | Path | Size/checksum |
|---|---|---|
| Generation package | `tools/stm32-toolkit/src/stm32_toolkit/generation/` | `__init__.py` 30 lines, managed_files.py 508 lines, configure.py 2043 lines (r002) |
| Root templates | `templates/cmake/` + `templates/vscode/` | 9 files, byte-identical to packaged copies (hashes in section 4) |
| Packaged templates | `tools/stm32-toolkit/src/stm32_toolkit/templates/` | 9 files, declared in `pyproject.toml` package-data |
| Generation tests | `tools/stm32-toolkit/tests/test_generation.py` | 273 tests |
| Doctor tests | `tools/stm32-toolkit/tests/test_doctor.py` | 10 new test functions (24 total, 589-line file) |
| Implementation report | `docs/openclaw/returns/STM32TK-0304-GCC-CONFIG/r001-implementation-report.md` | this file |

## 5. Security, privacy, performance, accessibility, and compatibility

- Security checks (all automated, all pass):
  - Forged-plan defense at apply: exact `GenerationPlan`/`ProjectModel` types, `plan_version == 1`, container/scalar types, portable paths, exact generated-target membership, status/template name/version consistency, unique paths, Unicode `casefold()` collisions, bytewise sorted orders, per-file before/after byte-vs-digest consistency (length + SHA-256), blocker code/path/order validation, recomputed manifest bytes, recomputed `model_sha256` and `plan_id`, per-input and aggregate size limits, and serialized-plan bound — all before the first write.
  - Apply preflight: canonical root equality, fresh `load_project_model` + model-hash equality, byte-exact input revalidation (portable form, containment, regular file, bounded read, size + SHA-256), current-target state matching plan before-digests, staging containment (every component of `.stm32-toolkit`, `configuration-staging`, `<plan_id>` resolved and prefix-compared against the resolved root, checked before the fresh-model load and repeated immediately before staging and before each destination replace), existing-staging rejection (r002: every staging intermediate component is `lstat`ed and a non-directory intermediate is rejected with `GENERATION_TARGET_EXISTS` before any write), fresh re-plan metadata/raw-byte equality (`GENERATION_INPUT_CHANGED` for model/input/target changes, `GENERATION_PLAN_INVALID` for forged inconsistencies), and blocker refusal (`GENERATED_FILE_DRIFT` if any drift, else `GENERATION_BLOCKED`) before staging.
  - Atomicity: exclusive staging creation (`O_CREAT|O_EXCL`), staged/backup/temp writes flushed and fsynced, sibling temporaries replaced with `os.replace`, explicit `chmod` after replace (umask-independent exact modes), directory fsync on POSIX (skipped where unsupported), reverse-order byte/mode-exact rollback with created-parent removal, staging removed on success and pruned only when empty, rollback failure retains recoverable staging with portable paths. r002: the rollback guarantee now also covers every `_ApplyFailure` raised after the first real mutation — the original stable code and details are preserved on successful rollback, and `GENERATION_ROLLBACK_FAILED` (sorted portable paths, staging retained) is returned when the restore itself fails.
  - Input/evidence validation: portable lexical paths plus `resolve`-based canonical containment with in-root redirect acceptance and escape/loop/permission rejection; `lstat`/Windows reparse-aware state checks; sources/assembly/SVD/IOC must be regular files, includes directories; NUL/traversal/drive/UNC forms rejected at model load and re-checked at generation; fixed-section report must be a UTF-8 JSON object without duplicate keys, schema v1 with valid planId/gitHead, exact `.stm32tk.abs.%08x` sections, declared sources, positive lines, safe C symbols, collapsed duplicates, conflict/out-of-region rejection; every validated address is emitted verbatim as the output-section start address (`0x%08x`), so the linker places the section at the exact evidence address within its declared region; all bounded (8 MiB/file, 64 MiB aggregate, 1 MiB template, 8 MiB generated file, 64 MiB total/manifest, 64 MiB serialized plan).
  - Rendering: StrictUndefined Jinja, no autoescape, no filesystem loader, no globals/imports, only explicit scalar/list context plus the deterministic `json` filter; missing/oversized/undefined/encoding/oversized-render all fail closed with `GENERATION_TEMPLATE_INVALID`; every generated JSON renders native booleans/null/arrays through the filter.
  - Doctor: exactly `[resolved_code, "--list-extensions", "--show-versions"]` through the existing bounded process machinery (8 KiB retained, 5 s timeout, no shell, fixed argv); a failed probe never claims missing; no GUI, no installation, no settings change, no extension-directory or home scanning.
- Privacy/redaction checks: `OperationResult` success data and every failure `details` contain no absolute root path, username, environment value, host exception text, rendered content, or source bytes; verified by `assert str(root) not in json.dumps(result.to_dict())` style tests for success and every failure family; the report contains no self-referential SHA.
- Performance measurements (12; `time.perf_counter`; 3 warm-ups then 20 plan runs / 10 fresh apply repos; min/median/max in ms): plan **274.3 / 276.1 / 287.7** (budget 500), apply **37.9 / 38.3 / 39.3** (budget 1,000) on Linux 7.0.0-22-generic x86_64, CPython 3.10.11, Jinja2 3.1.6, 4096-byte-block filesystem. Fixture: 1,000 declared sources + 100 fixed sections; apply fixture 9 files over 10 fresh disposable projects. No timing assertions added to the test suite.
- Accessibility/input checks: `NOT_APPLICABLE` for UI; stable English errors with structured `details` for AI/CLI adapters.
- Compatibility checks: CPython 3.10+ (verified on 3.10.11 and re-verified on system CPython 3.14.4: 287 focused passed on both); generated files are UTF-8/LF with exactly one final LF, `indent=2`, `ensure_ascii=False`, and `/` separators on every host; Windows path forms rejected on every host; casefold collision detection covers both POSIX and Windows semantics; template resources load from an installed wheel outside the repository (verified in a fresh venv); doctor/extension parsing is case-insensitive; the 17 suite skips are pre-existing Windows-only platform skips; r002 test injections are platform-neutral (`Path.parts`/`Path.name` matching, `PermissionError` injection, real NTFS junction directory redirects, deterministic reparse/resolve file redirects, hit-counted races) and require no administrator privileges or developer mode; real Windows NTFS junction behavior, Windows atomic replace/rollback, and real CMake/GCC configure remain Codex gates.

## 6. Blockers and residual risks

- Blockers: `NONE`.
- Residual risks:
  1. Real NTFS junction escape, Windows atomic replace/rollback, and the full Windows focused/full suite are verified on Linux by real symlinks and deterministic reparse/junction-injection tests only; the real Windows gate remains Codex's. r002 removed the privilege/separator dependence (real junctions for directory redirects, reparse/resolve injection for file redirects, `Path`-based matchers, `PermissionError` injection, actual-mode comparison, hit-counted races) so the suite is expected to run unprivileged on Windows 10/11.
  2. Real `cmake --preset arm-debug`/`arm-release` configure of a generated fixture is deferred to Codex's Windows environment (CMake 4.3.1, Ninja 1.13.2, ARM GNU toolchain); OpenClaw verified the generated contracts structurally and by exact snapshots — including the new absolute-address `.stm32tk.abs.<ADDR> <ADDR> (NOLOAD)` output sections, which are standard GNU ld syntax — and the absence of host paths and shell tasks.
  3. The `_map_fresh_error` `GENERATION_MODEL_INVALID` branch, the destination-build manifest-read OSError branch, and the staging-intermediate `lstat` OSError branch are defensive (the surrounding preflights normally catch those states first) and are covered by unit tests or left fail-closed rather than end-to-end flows.
  4. `os.chmod` after `os.replace` makes created/replaced modes deterministic on every host (umask-independent); a chmod failure rolls back exactly like any replace-phase failure.
- Follow-up recommendation: `NONE` (later build/CLI/MCP modules may consume `stm32_toolkit.generation`; the generated tasks are forward-compatible declarations for those modules).

## 7. Author checklist

- [x] Accepted base and code head are full SHAs (`8a627e95732bff069bef76e75c6b5f6fa992c6f4`, `2b9c57436bc7237c6b8f6ff74e6ef2a11c61a659`).
- [x] Final head will be returned out of band after this report commit (PR metadata + return message).
- [x] Inventory matches the complete implementation diff and report addition (25 section-5 paths + report; r002 revision diff = exactly 4 paths).
- [x] Every required OpenClaw gate has direct observed evidence on the code head (r001 TDD RED exit 2 with exactly the two expected collection errors; r002 revision RED 9 failed with the rejected behaviors; focused 287 passed; full 784 passed/17 skipped; branch coverage 92%; compileall; diff check/scope; dependency Jinja2-only; template identity with updated linker hash; wheel load; read-only; atomicity/rollback incl. semantic-failure rollback and rollback-failure retention; performance plan 276.1 ms / apply 38.3 ms medians).
- [x] Other-environment gates are accurately attributed or deferred (Windows suite + real NTFS junction + real CMake/GCC configure `DEFERRED_TO_CODEX`; visual `NOT_APPLICABLE`).
- [x] No credentials, private data, caches, build output, binary fixtures, temp projects, or unredacted diagnostics are committed (venv outside the repository; disposable projects under `/tmp` and pytest temp dirs removed; `.coverage`/`__pycache__`/`.pytest_cache`/`*.egg-info` ignored).
- [x] No unrelated file, agent instruction, approved work order, or remote policy changed (diff scope verified; `AGENTS.md`/`OPENCLAW_START_HERE.md`/work order/upstream modules untouched; no skip/xfail added).
- [x] Every instructional value in this report is replaced with actual evidence; no self-referential final SHA and no moving commit totals.
- [x] Overall status `IMPLEMENTED` (revised r002): complete suite exits 0 on CPython 3.10.11 (784 passed, 17 skipped), branch coverage 92%, performance budgets pass, wheel/resource gates pass, all four Codex rejection items fixed, Windows gates `DEFERRED_TO_CODEX`.
