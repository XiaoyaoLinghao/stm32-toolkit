# Controlled Fault deployment and first verification

Current status: **DEPLOYED / PASS_PHYSICAL_CONTROLLED_FAULT_SINGLE_SCENARIO**. The newly authorized guarded invocation passed; see the final section. The first invocation remains **TERMINAL_STOPPED_INFRASTRUCTURE** in its original evidence. User authorized "开始部署后进行实机验证" and confirmed connected probe, blinking D4, and stopped IDE/Keil debugging. Main owns this deployment and test entry; the accepted Luna/max product implementation remains unchanged. Source is `70ed9c70075445d66d9229a1420817f604843fd2`, with product bytes equal to accepted `333f456dfd0382ceb6b1904e5e29a7592a9ebda9`. First-invocation evidence: `D:\codex-tmp\fault-deploy-20260911`.

One clean LF build using the existing closed wheelhouse passed bundle verification. New persistent DataRoot `D:\stm32tk-data\fault-controlled-20260911` followed Check missing -> Bootstrap exit 0 -> Check healthy/matching exit 0. Runtime is `runtime\0.9.0` beneath that root. Manifest SHA256 `fd4b99c47ca3abb851442f6f8549a4c7e5a2e8f306dd3aae55e364d751331514`; runtime-state SHA256 `dcf3a33d9ab6e27d2e03465b3a8614bcb4b339fa7d1204dc466ae790e85f4fa9`. All 145 Toolkit/Monitor package files match their verified wheels, final pyocd.exe reports 0.45.1, and installed production `_prepare`, `_load_fresh_firmware`, `_load_flash_result`, `_validate_flash` pass offline for the existing P2/session/probe/build/ELF receipt. Eleven preserved firmware/runtime/ownership files remained byte-identical. No old session, ticket, action or runtime-state was copied.

The reviewed execution card authorized exactly one controlled Fault CLI invocation, with a 360-second process ceiling and the existing 45-second restoration budget. It did not authorize flash/reset, separate probe enumeration, extra sampling, or automatic retry. The main agent added a run-local exception capture wrapper because the CLI otherwise maps unknown exceptions to a generic code. The wrapper's capture and --help checks passed, but those checks missed Windows child-process importing of the entry script.

The one invocation at 2026-09-11T08:28Z ended in 11,321 ms, CLI exit 2 / PROBE_TIMEOUT, message "Probe worker operation timed out", with no Fault report and no controlledSnapshot. The 360-second guard did not fire. Raw exception boundary locations prove the cause: `hardware_workflows.py:748` -> `probe/supervisor.py:98` -> `probe/worker.py:371` -> `worker_windows.py:214,222` -> Python 3.12 `multiprocessing/spawn.py:140` (`_check_not_importing_main`) raised RuntimeError in the child. The unguarded `capture-fault.py` executed CLI again while the spawn bootstrap imported it as `__mp_main__`. The parent then timed out waiting for the ready message at `worker.py:386,410`.

Classification: **INFRASTRUCTURE / test-entry defect owned by the main agent**. Last proven phase is worker subprocess bootstrap. The worker entry `_worker_main`, backend construction, service start/lease acquisition, probe enumeration, attach, firmware readback, halt, analysis and resume were not reached. This failure supplies no evidence against the board or the accepted controlled Fault product. The original wrapper and its hash remain preserved in fault-argv.json; no later edit replaces the failed artifact. Consumers-after is empty, the new probe registry record is absent, and all eleven preserved baseline hashes match. No post-failure hardware operation occurred. Final user D4 observation is recorded separately when returned.

The corrected run-local wrapper is prepared under `D:\codex-tmp\fault-verify-20260911-02\capture-fault.py`, SHA256 `5ec23f17b9bda57380de7f2f764db1ca38c6d89cf4a96336242f09902c30da36`. All CLI execution and wrapper installation are now inside the __main__ guard. With the installed runtime, the actual `WorkerWindowsSpawnProcess` successfully spawned a pure-software `abs(-1)` child and exited 0; capture self-check and CLI --help also passed. These are offline checks, not a physical retry or product regression run. The corrected entry has not invoked hardware and needs a new one-call hardware authorization; no repackaging or redeployment is needed.

T9, attempt7, existing P2 and 100ms evidence remain valid in their recorded scopes. FullFault physical validation, other remaining Task7 observations, T10, Task11/12 and VS10-A remain incomplete. Policy-denied cleanup targets from prior verification remain untouched.

Independent read-only acceptance_regression_ledger review confirmed the bootstrap-only failure classification and then returned ACCEPTED_DOCUMENTATION for the corrected guarded entry and SOP increment. The next one-call execution card and exact planned argv are prepared in the successor evidence directory with authorization explicitly pending. Deployment and firmware evidence are reused; the failed invocation is not erased or relabeled as physical PASS. No further hardware was performed.

## Newly authorized guarded invocation: physical PASS

The user approved the precise prepared single-call boundary with "可以开始". Evidence is `D:\codex-tmp\fault-verify-20260911-02`: `preflight.json`, `fault-argv.json`, exclusive `fault-dispatched.json`, raw `fault.stdout.json`, empty stderr, `fault-exit.json`, process inventories and `final-result.json`. The runtime-state/capture hashes, fixed firmware and receipt remained unchanged; preflight found no active debug/worker consumer or new registry owner. Deployment was reused without reinstalling or flashing.

Exactly one invocation finished at `2026-09-11T08:42:15.9289400Z`, CLI exit **0 / OK**, elapsed **13,347 ms**, with no outer timeout. The real complete Fault report and controlledSnapshot prove **running -> halted -> running**. Halt and resume each dispatched successfully. CFSR, HFSR and SHCSR were zero; DFSR was 1 (`halted`). PC/LR resolved to `RS485_3_Send_Data`; no exception stack frame was produced. These are snapshot facts, not a general assertion that the firmware has no faults.

All five cleanup stages succeeded, including backend close and lease release. The registry is `released`, the entry process exited, and no debug/worker consumers remained. Worker-stop proof is the successful installed backend-close path: `ProbeBackendWorker.close` invokes `abort_owned_execution`, which checks child liveness before returning; the process inventory corroborates this. No independent child exit-code field was exposed. All eleven preserved baseline files remained byte-identical. Raw stdout SHA256: `d4bd95ae93863dc923fdc597e76c22ce44f1546e05441c5c15e3a4a864da8874`.

After completion the user separately confirmed D4: **"在闪烁"**. No extra board read was made for that observation. The single controlled FullFault physical path passes. Remaining Task7 observations, T10, Task11/12 and VS10-A remain incomplete; historical T9, attempt7, P2 and 100ms evidence retain their existing scope. Evidence is retained for acceptance; no disposable test tree was created and no previously denied cleanup target was touched.

Independent read-only acceptance_regression_ledger review returned **ACCEPTED_SINGLE_SCENARIO** after checking the response, exit, firmware/argv binding, registry and process evidence. The reviewer performed no hardware operation and retained the stated worker exit-code and acceptance-scope limits.
