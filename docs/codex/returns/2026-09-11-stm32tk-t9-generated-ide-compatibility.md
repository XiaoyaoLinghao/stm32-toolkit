# T9 generated IDE configuration correction

Status: **SOFTWARE_COMPLETE_HARDWARE_PENDING**. The user requested “开始修正”. The bounded software change and offline checks are complete; no deployment, hardware, IDE session or remote action was performed. The currently deployed source remains `814b1683d2f562ce1bb2db464ee4c572cb31d658`; existing runtime, firmware, receipt and handoff baseline hashes remain unchanged.

Accepted base: `f27931ff85be33bb0466b69300777a9c07455ed4`. Luna/max product CodeHead: `0de8bd756ba145c6626e500dcf6c4cfb02612cfb`; final implementation head including the skill usage update: `0fe93ef140e321bad545dbc411d5268d57b08961`. Integrated candidate: `894a03d0b241bd139ae3ea890684ca29c16f6edb`. Main owns design/integration and independently reviewed the full base-to-final implementation and integration diff in `D:\workspace\stm32tk-t9-generated-ide-compat-review`, detached and clean at the reviewed head. Verdict: **ACCEPTED for software scope, no blockers**.

The exported canonical `CortexDebugAttachContract` constructor and exact seven-field serialization remain compatible, including raw `boardId`; legacy two-argument `HandoffTicket` construction remains supported. A generated ticket now also provides `cortexDebugLaunch`, with schemaVersion 1, an explicit Cortex-Debug 1.12.1 / PyOCD 0.45.1 profile, and a derived launch configuration. The profile is a supported consumer declaration, not automatic environment detection. The caller must verify actual versions before use and stop on a mismatch.

The new configuration supplies canonical absolute cwd, preserves the exact raw probe ID as one `--uid` argv element with `--connect attach`, omits the obsolete launch `boardId`, and supplies the matching GDB ready regex. Target/executable and canonical identity remain unchanged. New cwd validation occurs before reservation/stop/state publication; serialization performs no filesystem lookup. Existing companion schema, ticket/lease transitions and cleanup remain intact, including repeated begin. Returned nested containers are fresh. The public debugging skill and standard procedure now select the versioned configuration instead of asking the user to manually project the identity fields.

## Verification

- Owner-reported handoff module: 127 passed, exit 0; strengthened control-root check: 1 passed, exit 0. Attribution and source heads are retained in the local result. The earlier direct-API-change RED belongs to a superseded design and is not counted as a regression for this implementation.
- Main independently ran five focused tests covering legacy construction, launch projection, fresh begin, repeated begin and pre-ownership root rejection: all passed. Complete diff checks passed; integration preserves the implementation product/test/skill bytes.
- Main reused the installed Cortex-Debug methods in Node and the installed PyOCD argument parser offline. Baseline canonical launch reproduced the missing-folder `uri` error, rejected `--board`, and failed to match the listening message. The new generated projection passed cwd resolution with no folder object, exact raw-ID parsing including spaces, attach mode with reset-run false, both GDB ready variants, and STDIO rejection.
- This invokes only configuration serialization, existing extension methods and argument parsing. It does not run handoff begin, a GDB server, probe enumeration or hardware. The final skill-only commit did not change verified product bytes and did not trigger another test cycle.

Evidence: `D:\codex-tmp\t9-generated-ide-compat-20260911`, including `baseline-consumer-red.json`, `actual-extension-methods.json`, `consumer-invocation.txt`, `consumer-green.json`, `independent-focused.log`, `independent-focused-exit.json`, `result.json` and hashes. The inline consumer invocation is a bounded reuse of existing functions, not a new product diagnostic framework.

The implementation owner reported its temporary test directories cleaned. Automatic policy rejected the main agent's native cleanup of the new `pytest-review` and `review-temp` directories with `blocked by policy`; they remain retained, without a bypass or retry. Useful evidence and all historical physical PASS records remain preserved.

Next: separately authorize and prepare candidate deployment plus one real IDE check using the newly generated `cortexDebugLaunch.configuration`, following the standard procedure. It must have fresh ownership and a matching environment profile. Do not repeat firmware flashing, accepted Target runs or CLI/MCP parity merely because these documents changed. Full T9, T10 and VS10-A remain incomplete.
