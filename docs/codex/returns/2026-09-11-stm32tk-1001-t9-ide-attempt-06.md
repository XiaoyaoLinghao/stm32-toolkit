# T9 attempt 06: handoff succeeded, IDE start failed

Status: **TERMINAL_STOPPED_IDE_START_ERROR**. One authorized handoff begin succeeded; the user subsequently reported `Cannot read properties of undefined (reading 'uri')` after F5. The probe reservation remains `externally-owned`; no successful IDE attach/detach, end/reacquire or post-handoff read is claimed. T9, T10 and VS10-A remain incomplete.

The user requested explicit rules for when the standard procedure changes or steps may be omitted, followed by this test, and confirmed availability to operate real VS Code. SOP applicability delta `a1f2c010..b4dfcd37` and the bounded IDE adaptation delta `b4dfcd37..a1aae577` were independently ACCEPTED. Primary owns this hardware lane and external configuration; no product implementation, deployment or remote mutation occurred. Deployed source remains `5f036363383e6c85cc87426da1b4a1c20dfe7acc`.

Offline existing `_prepare`, `_load_fresh_firmware`, `_load_flash_result`, `_validate_flash`, `_read_state` proved the current P2/receipt binding and absence of a prior active handoff. The receipt still belongs to `vs10a-t9t10-p2-20260911-04`, build `77d787ee83f744831316f9531b825935ef276520472221a8174f7a7d4cfe57f9`, ELF SHA256 `10df523425dbe8567d5876e5e790714d1314a5dd3d545e4d43a063c300fd6ead`. Initial registry state was released with no matching debugger owner. Same-context MCP stdio initialize/list-tools succeeded offline; it did not call a hardware tool.

The IDE preflight found an ENVIRONMENT compatibility blocker before hardware: Cortex-Debug 1.12.1 generates `gdbserver --board`, whereas PyOCD 0.45.1's main CLI rejects `--board` and its legacy launcher rejects the preceding `gdbserver`. Existing parser invocation reproduced the old parameter rejection with exit 2 and accepted `--uid <raw ID> --connect attach`, with reset_run=false. Target support was verified offline using the existing pack population function and local STM32F4 pack 3.1.1. This does not equate that pack to the manifest's 2.17.1 declaration.

The independently reviewed, run-local adaptation preserves the complete actual handoff response, omits only launch.boardId, and passes exactly that returned raw boardId through serverArgs to `--uid`; other identity fields remain unchanged. It uses the installed D runtime pyocd.exe and actual pack with explicit connect attach. No launcher script, dependency change, firmware modification or default product configuration fix was introduced. Results must identify the adapted IDE path; the original generated configuration's direct compatibility remains unresolved.

One normal public `debug handoff begin` exited 0 and returned a fresh ticket and actual Cortex-Debug identity. Its persisted state and registry reservation were retained, and `externally-owned` plus matching ticket were checked before requesting VS Code startup. No flash, Fault invocation, retry or under-reset fallback occurred. The external workspace points to `project-standard-math`; the requested startup used a D run-owned user-data directory and existing installed extensions, with auto-update and CMake auto-configure disabled. The debugger was not automatically started. Later logs prove this requested IDE process did not open successfully; the earlier statement that it had opened was incorrect.

Evidence root: `D:\codex-tmp\t9t10-t9-20260911-06`. Retained files include execution-card.md, offline-preflight.json, prior-flash-result.json, mcp-preflight.json, ide-parser-preflight.json, handoff-begin.json/stderr, begin-exit.json, handoff-state-after-begin.json, lease-after-begin.json, t9-attach.code-workspace and result.json. Actual ticket/raw probe ID stay in run-local evidence, not this report. The temporary directory and IDE user data remain needed while the test is active.

The planned IDE attach/detach and subsequent end/reads are now stopped by the reported failure. Toolkit must not access the probe during the external reservation. Retain the original ticket for an explicitly authorized recovery; do not issue a duplicate begin, clear the reservation, steal, or automatically retry. Final cleanup and board state remain unconfirmed.

Evidence reuse: Target 04 physical PASS and deployed checks remain valid; -12 covers historical same-firmware typed testtime activity, PE4 changes (30 transitions) and Monitor observation. It does not supply a Fault report or T10 physical publication. D4 approximately one-second visual cadence and the original clean-project clause still need explicit reconciliation; `gitDirty=true` is neither a clean-tree proof nor a root-cause diagnosis.

## F5 failure: initial offline evidence

The user confirmed the URI error appeared after F5. No hardware operation was made during this follow-up. Process inspection found no currently running PyOCD/GDB debugger; this snapshot alone does not prove no earlier transient connection. Registry and handoff state remain externally-owned, and the canonical P2 flash receipt still matches the saved pre-begin hash.

The requested C-installed `new_Code.exe` logged `vscode-updating` mutex contention at 09:47:48 and startup failure after 31,258 ms at 09:48:19. The actual active IDE is `D:\Program Files\Microsoft VS Code\Code.exe`, version 1.129.1, using the existing Code user profile. That profile's extension log records `onDebugResolve` at 09:53:15. The captured log excerpts do not contain the exact URI exception stack, so the updater failure must not be asserted as its cause. The URI configuration path is being checked separately against the installed extension.

Minimum evidence is retained under the same run root's `ide-failure` directory: original workspace configuration, requested-IDE main log, active extension/renderer excerpts, process snapshot, handoff/registry snapshots and summary.json. No installer, global extension, firmware or probe state was modified.

## Offline configuration correction

The installed Cortex-Debug 1.12.1 provider contains the concrete failing expression in `dist/extension.js:1`, zero-based character offset 80998: `let a=t.cwd||e.uri.fsPath;l.isAbsolute(a)||(a=l.join(e.uri.fsPath,a))`. Here e is the callback's WorkspaceFolder. The workspace-level launch omitted cwd; with undefined folder, the actual extracted expression reproduces the user's exact URI TypeError before symbol-file validation or debugger startup. A relative cwd also fails. The live popup stack was not retained, so this is a source-backed reproduction of the triggering configuration, not a recovered live stack or proof that no other earlier session ever touched hardware.

The only external configuration change is `cwd="D:/codex-tmp/stm32tk-vs10a-legacy-campaign/project-standard-math"`. Node replay of the actual installed expression now returns that existing absolute directory; structural comparison proves all other workspace/configuration fields unchanged. Evidence: `ide-failure/cwd-replay.json`, bundle SHA256 `db421806fc2f220f449a7a2575945ff2c51f8228955830ab10466ffefe674ee3`. Classification: ENVIRONMENT/IDE configuration integration. This is not a firmware or probe-identity correction and not a physical PASS.

SOP now explicitly requires absolute cwd for workspace-level launch and proof of actual IDE startup/loaded context before handoff. No F5, server launch, end/reacquire, hardware read/control or retry occurred during this correction. The terminal status and retained external reservation remain in force pending newly authorized continuation.

## Newly authorized continuation 07

After the offline correction, the user explicitly authorized the offered one-time IDE retry and normal-exit/reacquisition verification with “可以，继续测试”. The new record is `D:\codex-tmp\t9t10-t9-20260911-07`; 06 remains the historical failed attempt. Offline checks confirmed absolute cwd, unchanged raw-ID mapping and flash receipt, original ticket equality with both handoff state and registry digest, the actual D Code.exe processes, and no current PyOCD/GDB process. A snapshot of the corrected existing workspace and the new execution card were retained. No duplicate begin or new hardware operation was performed by the primary.

At authorization time the state was AWAITING_USER_SINGLE_IDE_RETRY under the existing external reservation. That continuation is now terminally stopped by the errors below; the previous retry budget is not permission to retry after failure. No end/reacquire or follow-up reads occurred.

## Continuation 07: GDB Server Quit, offline launcher blocker

The user first reported a missing STM32 Toolkit task, then `PyOCD: GDB Server Quit`. The P2 folder launch has pre/post handoff task references absent from tasks.json, but this does not explain the latest selected T9 configuration: the actual Peripheral Viewer log, lines 3–5, records session `6772eef0-42d8-4df1-a538-dff076a7db19` initializing with `T9 - Attach (no flash)`, absolute cwd, D runtime pyocd.exe, `--uid` and `--connect attach`, and no preLaunchTask, then capabilities and terminated. The previous task-selection uncertainty is superseded by this evidence.

The configured `D:\codex-tmp\stm32tk-vs10a-legacy-campaign\data\runtime\0.9.0\Scripts\pyocd.exe` has SHA256 `02751dac411be2c3b89d8f6607a6d3c742208bc2f7be3194cc8461bfd022698d`. At byte offset 108032 its embedded shebang points to `D:\codex-tmp\stm32tk-vs10a-legacy-campaign\data\runtime\.staging\0.9.0-235025733e1449719708b396f58283ed\Scripts\python.exe`, which does not exist. The expected interpreter is the existing final `runtime\0.9.0\Scripts\python.exe`.

One hardware-free invocation of the exact configured executable with `--version` exited 1 with empty captured stdout/stderr. The final runtime Python with `-I -m pyocd --version` exited 0 and returned 0.45.1. These checks prove a broken launcher entry before PyOCD can initialize through that missing interpreter; they do not execute gdbserver, enumerate a probe, or connect to hardware. Existing module/parser and Toolkit physical evidence did not cover this executable entry and remains valid within its original scope.

The original IDE gdb-server terminal stderr remains unavailable: computer-use window selection failed its identity check twice, including one refreshed selection, with `window id 18026694 no longer belongs to Microsoft.VisualStudioCode; current owner is Microsoft.VisualStudioCode`. No input or debug restart was sent. Therefore the launcher defect is a confirmed sufficient startup blocker, not a claim that every live failure or transient hardware stage was recovered. Live phase proven: debug adapter initializing/capabilities then terminated; successful server ready, probe attach and Watch remain unproven.

Evidence: `D:\codex-tmp\t9t10-t9-20260911-07\gdb-server-failure` contains exact version outputs, exit codes, embedded path/offset and captured effective-session events. Current status is TERMINAL_STOPPED_GDB_SERVER_QUIT. Original external reservation and valid P2/attempt 7/100ms evidence are preserved. No runtime/configuration/product bytes were changed, no deploy or hardware retry performed. The correction should address the launcher interpreter binding and verify the actual final-path executable offline before any newly authorized IDE retry; no staging-directory resurrection is proposed.

Subsequent user-authorized correction: the [installer and bounded local repair](2026-09-11-stm32tk-runtime-pyocd-launcher-repair.md) now pass offline. The original paragraph above describes the diagnostic turn before that repair. Real pyocd.exe now returns 0 / 0.45.1; the original attempt remains terminally stopped, external reservation unchanged, and no IDE/hardware retry or reacquisition has occurred.

## Continuation 08: PyOCD error, original output pending

After fresh user authorization “继续进行”, offline checks confirmed the fixed launcher bytes, configuration, original ticket/registry digest, unchanged critical files and no debugger consumer. The user was the sole IDE operator and then reported `testtime=不可用，PyOCD报错`. Status is TERMINAL_STOPPED_IDE_PYOCD_ERROR; no end/reacquisition, CLI/MCP read or retry followed.

The current launcher still matches the verified repaired SHA256. Existing extension logs show the correct T9 config initializing/capabilities then terminated (latest observed session `3c095ff4-249b-4327-a8bb-f67812f7c05e`); multiple sessions exist without a recovered per-action timing chain, so they are not all attributed to the single authorized attempt. No current Python/PyOCD/GDB process was observed. Handoff and registry remain externally-owned. Server readiness, successful attach/Watch, detach cleanup and board state are not proven.

Evidence is retained in `D:\codex-tmp\t9t10-t9-20260911-08\ide-failure` and result.json. Standard app logs do not contain the original PyOCD exception; the native UI tool still rejects window ownership validation. The user has been asked to copy existing gdb-server/Debug Console output, without restarting. Root cause remains unclassified pending that evidence; variable unavailability is not a diagnosis. Earlier physical PASS and the independently proven launcher correction remain preserved.
