# Sequential-lease publication correction accepted offline

Verdict: **ACCEPTED — software correction, not deployment or T10 completion.** User approved the specification and plan with "同意，可以进行修正". Accepted integration base `e3784aa8551faaba7ec1c67126ae9ad320c889de`; implementation start `b4f74af6dd18ed7086b239d3b15a18e674a9033f`; code head `72e9706c9cf60dc3204b351a98d3d01623125dd5`. Luna/max `monitor_lease_fix` implemented and tested; primary independently reviewed the complete base-to-code-head diff in clean `D:\codex-tmp\t10-lease-review` and verified the actual saved evidence. The integration branch was advanced locally; no remote action occurred.

Only `tools/stm32-monitor/src/stm32_monitor/replay.py` and its existing `tests/test_physical_publication.py` changed. Publication no longer equates Target and later Monitor leases. All stable identity checks and within-Monitor window/transcript/reference consistency remain. Each operation's lease is still validated through its existing authority. The immutable physical transcript already links its actual `test_run_id`; authenticated loading reopens the original Target through `TestRunRepository` and checks stable identity. This existing association preserves the Target lease separately, so a new duplicated target-lease schema field was not required.

## Verification

- The new distinct-lease regression reproduced `INCOMPATIBLE_IDENTITY` before the correction, then passed. The affected module passed **36 tests** with existing CPython 3.12.10 / `py -3.12`; the installed candidate environment has no pytest. No broad matrix was run. An initial test-path `FileNotFoundError` was corrected and retained separately; it is not counted as product RED.
- Owner command from the implementation worktree: `py -3.12 -m pytest tools/stm32-monitor/tests/test_physical_publication.py -q -p no:cacheprovider --basetemp D:\codex-tmp\t10-lease-fix-run\basetemp-green-full`. RED adds `-k sequential_leases` and uses `basetemp-red-v2`. PYTHONPATH contains that worktree's Toolkit/Monitor `src` directories; TEMP/TMP are the run root; PYTHONDONTWRITEBYTECODE=1.
- Primary used the existing public publisher entry with Monitor source from the exact clean review commit and Toolkit dependencies from the installed candidate runtime. It operated solely on an isolated copy of the actual stored TestRun and SQLite history, without creating a runtime service or accessing a probe. Publication returned OK; a fresh process authenticated the resulting reference successfully.
- Target run `target-v2-53b0c81ffeb50ab65bd6c0ad815ad273` retains `lease-203e4bda26ec46bd9b7424c26f36cfec`. Monitor run `6a8508ee-95ec-43f0-a638-73d37d7038c5` retains `lease-8adfb28e758f48759ce46b91594d85f6`; run-ref SHA256 `37e15560615974b49d041cd4386d3fbcbebaea76df395afd09581ae643739fc1`.
- All 32 original evidence files and their copied originals remained byte-identical. The production business evidence store was not published to or changed. This is offline verification using existing physical evidence, not a new physical PASS.

Owner logs: `D:\codex-tmp\t10-lease-fix-run` (`red-sequential-lease-v2.txt`, focused GREEN, module GREEN). Independent evidence: `D:\codex-tmp\t10-lease-evidence` (`copy-result.json`, source manifest, candidate module origins, candidate publication, fresh-reader/hash verification and isolated evidence copy). Keep these minimal records and candidate/review worktrees for deployment handoff; dispose of test basetemp/cache outputs after verification.

Cleanup was attempted only for four attributed basetemp directories and the implementation test `__pycache__`, with absolute-path containment checks. The execution policy rejected process creation with `blocked by policy` and no more specific reason. Nothing was deleted or retried; these temporary directories remain, recorded in `cleanup-disposition.json`. This does not change the software verdict.

The deployed runtime remains source `6250ef14035c053caa5ddc98c072c4be5b5e3650`. Deployment of this correction and production publication are pending. P4 source remains `8755ba8fd0c678f32d7d23e7d827e84317026429`, with the original P3 build/receipt and backup preserved. No new acceptance timer, hardware, flash or sampling operation occurred. Historical programming and 300-batch sampling PASS remain valid; P4/Diagnostic FixVerification/T10/VS10-A are not complete.

Independent documentation reviewer `t10_diagnostic_contract` accepted the new SOP recovery-order and current-state paragraphs and the report's evidence/authority boundaries. That review does not grant deployment or hardware authority.
