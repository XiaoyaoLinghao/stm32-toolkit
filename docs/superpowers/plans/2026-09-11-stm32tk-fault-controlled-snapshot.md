# Controlled Fault snapshot implementation

Approved contract: [controlled snapshot](../specs/2026-09-11-stm32tk-fault-controlled-snapshot.md). Accepted base: `71184baf04fb5df93dd18d69aee87a07f5dee27f`. User approved the governing behavior; the existing waiver permits this bounded implementation plan without another approval pause. Main owns integration and independent complete-diff review. The existing runtime_launcher_impl agent (Luna/max) is the sole product/test owner in a new isolated `codex/fault-controlled-snapshot` worktree. Other agents have read-only T9/T10 responsibilities; no overlapping writes. No remote, deployment or hardware authority.

## Frozen implementation and lifecycle

1. Add default-false Fault option through request/CLI/MCP. Leave omitted-option delegation exactly on its original path, including existing seam signatures. Validate exact boolean before starting a service. Explicit expected operation level is keyword-only, defaults OBSERVE, permits CONTROL only when selected and requires the actual endpoint to match. Update both firmware binding and Fault initial/final endpoint checks; reject MODIFY and invalid values. Do not touch read.py or Fault core-register/stability semantics.
2. Implement one Fault-only CONTROL lifecycle in hardware_workflows.py. Reuse `_one_shot` for service/client ownership if its error/detail handling permits the approved report contract; otherwise reuse the existing owned-await/cleanup helpers without duplicating a framework. Bind with the expected CONTROL level; get exact current target identity, match debug target/device and selected probe, and derive authorization project/revision/build/ELF from DebugFirmwareBinding. Use the same supervisor authorization store consumed by the service. A new authorization is prepared for each operation; call target_control directly so intermediate halt does not close the client.
3. Verify running, request halt once, verify halted, analyze once, restore once and verify running before service cleanup. A successful analyzer result cannot mask failed restoration or cleanup. Track whether halt was dispatched separately from whether it acknowledged, so ambiguous cancellation can be reconciled. Before an ambiguous restoration, require the original identity and positive current state in the still-owned session. Never reconnect, replay halt/resume or issue control after identity/state became unknown. Preserve the initiating analyzer/control error alongside restoration facts in details. Re-raise cancellation after bounded owned cleanup, following existing cancellation semantics; tests must prove no late second control or lost ownership.
4. Existing RPC deadlines remain unchanged (target_control currently 30 seconds). Give restoration one named absolute 45-second budget including its state/identity/authorization/control/verification awaits; it is a recovery budget, not an extension that converts an expired initiating operation into PASS. No timeout extension or loop-until-success. Reuse existing supervisor/client cleanup deadlines; do not invent a new supervisor. Future hardware execution needs a card sized for this lifecycle; the previous IDE 45-second outer wrapper must not silently be reused.
5. Keep Fault report schema unchanged. Add only `details.controlledSnapshot` lifecycle facts: mode=`halt-for-analysis`, before/analysis/after verified state (or unknown), halt/resume outcome and cleanup facts. Include stable initiating error code when restoration produces HARDWARE_CLEANUP_FAILED. Never publish tokens, endpoint credentials or tickets. Default-mode envelope remains unchanged.

Integration clarification before implementation: CONTROL binding may make its existing two attach calls before halt, but the analyzer's initial/final attachment checks must not reattach afterward. Implement the approved CONTROL-specific live target_identity validation described in the specification, preserving full first/final identity equality and the original halted-register checks. Add a regression whose fake backend actually resumes on reattach (or equivalently asserts no attach after halt) and invokes the production analyzer entry; a mock that leaves halted across every attach would conceal the defect. This adds only attachment-identity checking inside the already-owned debug/fault.py and test_fault.py; service/backend scope and public behavior are unchanged.

Keep the prior normal 100kHz SWD worker configuration for the Fault-only CONTROL path by reusing `worker_config.for_observation()` while retaining CONTROL service permission. Merely changing the operation level would bypass `_make_supervisor`'s OBSERVE-only configuration selection and accidentally use the 1MHz default. Preserve the target profile; do not change global worker defaults, backend, or unrelated CONTROL/MODIFY paths. Verify the constructed configuration offline.

## File ownership and verification

Owner may edit only src/stm32_toolkit/{hardware_workflows.py,cli.py,mcp_server.py,debug/firmware.py,debug/fault.py}, existing tests/{test_hardware_workflows.py,test_cli_hardware.py,test_mcp_hardware.py,test_debug_firmware.py,test_fault.py}, and skills/debug-firmware/SKILL.md, plus the single implementation report below. Paths under tools/stm32-toolkit except the skill. If a real public schema test in test_mcp_server.py or test_mcp_roots.py is directly affected, return the exact need to main before extending ownership. Probe service/protocol/backend/model, debug read/model and unrelated modules remain outside scope.

Reuse current fixtures and actual authorization store where relevant. Prove public option forwarding/default omission/invalid bool; strict operation-level defaults and mismatch; running→halted→analyze→running; identity or binding failure before control; fresh per-operation authorization; analysis failure restored; uncertain halt with known halted/running/unknown/changed identity; resume failure never PASS; cancellation during halt/analyze/resume with no duplicate control and positive owned close/stop evidence. No simulated result may be labeled physical. At least one test must exercise real binding/analyzer entry points with explicit CONTROL and unchanged halted checks, rather than mocking away the integration conflict.

Run the five affected modules once after focused development checks; use the existing D-based Python/test environment, process-scoped TEMP/TMP and pytest basetemp under `D:\codex-tmp\fault-controlled-snapshot-20260911`. Do not use or recreate old C temp/tmp. Preserve minimum failures, remove only attributed disposable outputs after they are no longer useful, and do not retry previously denied cleanup targets. No new generic script/framework, release matrix or packaging.

Return one local code commit, test command/count/exit and remaining limitations; write `docs/codex/returns/2026-09-11-stm32tk-fault-controlled-snapshot.md` with accepted base and CodeHead (never its own report SHA). Main reviews the entire base-to-CodeHead diff in a separate clean worktree, runs only risk-focused integration verification if needed, and accepts or returns concrete findings to the same owner. After two nonconvergent rounds reconsider design. Final software verdict cannot close physical Fault/Task7/T10/VS10-A evidence.
