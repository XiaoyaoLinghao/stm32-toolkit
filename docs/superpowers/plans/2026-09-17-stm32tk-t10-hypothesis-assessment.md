# T10 supplementary assessment implementation plan

Status: proposed, dependent on approval of the accompanying [specification](../specs/2026-09-17-stm32tk-t10-hypothesis-assessment-design.md), including its explicit retrospective acceptance amendment. Accepted base: `f65302c72817a8f4746bcc363ff60f3c4cbffb57`. No hardware or remote actions are included.

Primary is design/integration/evidence/cleanup owner. One Luna/max implementer owns the coupled product change and targeted implementation tests, in an isolated worktree under `D:\codex-tmp\t10h-0917`. Independent review has separate ownership; no recursive delegation or overlapping writers.

1. Freeze the exact selector shape, scalar validation, source authentication, reference publication and re-read rules from the approved specification. Pin an implementation branch with `codex/` prefix at the accepted base plus only approved design/report commits. Do not edit the deployed runtime or original production records during implementation.
2. Implement only the missing immutable Monitor observation path in `tools/stm32-toolkit/src/stm32_toolkit/diagnostics/model.py`, `diagnostics/events.py`, `diagnostics/store.py`, `diagnostic_workflows.py`, `monitor_analysis_contract.py`, and a minimal reused-reader seam in `acceptance/continuation.py` if required. Existing public plan/assess adapters remain the entry point. Implementation must stop for design review if it requires new commands, generic plugins, a second persistence layer or broader identity exceptions.
3. Use existing Diagnostic/continuation/analysis test modules for the bounded positive, tamper/identity and legacy checks. Generated files and basetemps stay under the short run root. No release matrix or hardware test is needed for the pure contract change. The implementer returns actual CodeHead and retained logs and does not accept its own change.
4. Primary and the independent reviewer inspect the complete accepted-base-to-CodeHead diff in a clean worktree. Reuse the installed public CLI/API sequence for a single verified real-evidence-copy integration run. A new generic runner is forbidden; any necessary data-preparation entry must justify why the current entry is insufficient before creation, and must be independently reviewed. Verify original evidence hashes and fresh-process assessment reads.
5. Only after software acceptance and explicit deployment authority, deploy one new candidate and verify its installed identity once. Prepare all three hypotheses, the full plan, expected facts and scoped rationales before beginning a fresh production supplementary session. Execute the fixed sequence offline, stop at the first error, preserve the old terminal Diagnostic/G/H chain and re-read the new record in a fresh process.
6. Reconcile original T10 Steps1–6 with exact P3/P4 Git/build/ELF/InputSnapshot, typed/SVD/Monitor/source/schematic evidence, source-change authorization and its negative check. Mark only satisfied clauses. Report the supplementary timing and residual uncertainty explicitly; never reclassify a retrospective assessment as an original pre-fix action. Task11 evidence packaging and Task12 complete-diff acceptance remain separate.

Authority to approve this plan/specification does not grant physical access, source reflashing, push/PR/merge/tag/release or cleanup outside the approved run root. Retain all prior cleanup holds. Clean only new disposable artifacts after retaining useful logs and evidence; report any cleanup rejection without bypass.
