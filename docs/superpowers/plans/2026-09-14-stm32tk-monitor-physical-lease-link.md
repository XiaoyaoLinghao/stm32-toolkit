# Bounded physical publication lease correction

Status: proposed, awaiting user approval together with the matching specification. Accepted integration base: `e3784aa8551faaba7ec1c67126ae9ad320c889de`. Primary owns design/orchestration/review; one Luna/max implementation owner uses an isolated worktree under `D:\codex-tmp`. Firmware owner retains the already prepared P4 backup and does not edit firmware during this slice. No remote authority.

1. Change only `tools/stm32-monitor/src/stm32_monitor/replay.py` and `tools/stm32-monitor/tests/test_physical_publication.py`. First reproduce the sequential distinct-lease rejection using the existing physical TestRun/history factories; their current shared `lease-01` fixture masks the serial lifecycle. Preserve authentic per-operation lease values and all stable identity predicates according to the specification. Do not weaken within-window/transcript/reference checks.
2. Reuse existing publication tests for same-lease compatibility, all other identity mismatches, mixed/tampered history and exact/conflicting retries. Add only missing distinct-lease coverage and assertions that the Monitor reference retains its own lease while the Target root is unchanged. Run this affected module; broaden only for a concrete failure or changed dependency.
3. Primary independently reviews the complete accepted-base-to-final-head diff in a clean worktree. Reuse the actual stored failed Target/history evidence in an isolated run-owned evidence copy through the candidate's existing public publisher. Compare original artifact hashes and the resulting Monitor lease/provenance. This is offline reuse of existing physical evidence, not a new physical run. No board connection is needed to prove this correction.
4. After acceptance, update the standard test procedure to publish and validate each stored Monitor window before starting the expiring source-change attempt. Reuse valid programming/sampling evidence. Candidate deployment and later P4 physical actions retain their explicit boundaries; no full release matrix, automatic deployment or repeated hardware for this publication defect.

Preserve the current P3 source/build/flash set and original TestRun/history. Keep new test outputs and the minimal reproduction under `D:\codex-tmp`; cleanup owner is primary. Do not start a new AcceptanceAttempt until this publication path and the remaining Diagnostic request chain are ready.
