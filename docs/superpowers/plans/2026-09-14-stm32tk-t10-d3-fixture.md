# T10 D3 fixture implementation plan

Governing specification: ../specs/2026-09-14-stm32tk-t10-d3-fixture-design.md. User approval is the 2026-09-14 instruction to execute the proposed D4/D3 separation. Toolkit baseline28f9b8119a768c5ff0499af2852ff60b6826b9ea; firmware base2bfa4bd81dc7474130e2d2c5acdad0162e15eb04. Primary owns design, docs, independent complete-diff review, integration and cleanup of main review artifacts. Only Luna/max agents implement.

Wave1 independent ownership after shared contract freeze:
- Firmware owner: isolated D:\codex-tmp\t10-d3-fw, branch codex/t10-d3-fixture. Own Main/Main.c, USER/VS10/vs10_target.c, tools/task8_offline_verify.py and its existing fixture JSON only. Preserve non-UTF8 source bytes/CRLF. Offline candidate preparation explicitly restores old P3 periodic LED1=1 to LED1=!LED1 and adds initial/periodic LED0=0. Commit this new D3-P3 before building. It is the future physical before-firmware; only the subsequent D3-P3 to D3-P4 authorization is limited to the periodic LED0 change. Rename logical case/counter/message predicates consistently to PE3; retain original protocol machinery. Adapt existing verifier/fixture without replacing it with a new framework. Build once using existing public build/arm-debug command and existing toolchain, all generated files inside worktree. Verify ELF/map reservation, native case digest, entry/symbol evidence using the adapted existing verifier. No hardware. P4 may be modeled solely in offline tests; do not commit or switch the actual fixture to P4.
- Entry owner: new D:\codex-tmp\t10-d3-20260914\entries only. Copy the existing reviewed monitor-entry/t10_monitor_entry.py and target-entry/prepare-source-declaration.py from D:\codex-tmp\t10-acceptance-20260911-01 unchanged first; preserve before copies/diffs. Adapt only D3/PE3 failure/fix and PE4 liveness predicates, the exact future source-change diff and future-root/session defaults. Preserve guards, identity/pagination/fail-closed behavior, finite budgets and original evidence. Reuse existing offline entry checks with bounded synthetic role records for D3; test happy roles plus stoppedD4/constanttesttime/wrongPE3/nonemptycursor negatives. No physical-shaped evidence publication. No product repo files, firmware, runtime or hardware access.

Primary independently reviews complete firmware base-to-CodeHead and complete entry-original-to-final diffs; use a separate clean review worktree for the firmware commit. Reconcile immutable build provenance and scope; never change a generated pin to satisfy a comparison. Run only affected meaningful verification, no old deployment/full matrices. Firmware worktree and run evidence remain under D:\codex-tmp. No approved historical artifact is overwritten. Worktree source audit, role predicates and actual future source-change state remain separate from physical acceptance.

After acceptance, update current standard-test-procedure with the D3 electrical map/case/role rules and new candidate pointers, with independent review of changed clauses. Keep latest hardware result terminal-stopped, deployed source70ed, lockup mapping fix integrated but not deployed, T10/VS10-A incomplete. No push/install/deploy/flash; those require separate authorization.