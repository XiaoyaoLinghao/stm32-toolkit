# VS10-A final-review correction plan

Design: `2026-09-18-stm32tk-vs10a-final-review-corrections-design.md`. Accepted base: `d11462ce361ed529baf142590006cb70fc87aee7`. User's active goal and explicit repair/retest authority cover this bounded work; no new permission or remote authority is inferred.

1. Target Luna/max owner uses `D:\codex-tmp\t10h-0917\tf` on `codex/vs10a-target-final-corrections`. Add only meaningful regression cases for delayed-flash host anchor, V2 partial-failure evidence/identity/bytes/error, and mixed-version discovery with a pure inventory sentinel. Record actual RED then implement within the frozen Target files. Run new nodes, then existing V2 runner/physical and V1 regressions affected by the shared runner. Tests/caches/logs remain under short `t10h-0917` children.
2. The current Luna/max entry owner finishes the already assigned run-local boot adapter, then owns `D:\codex-tmp\t10h-0917\kf` on `codex/vs10a-keil-final-corrections`. Add the unrelated-footer negative and valid real-layout positive; implement the bounded footer and repair the three fixture files. Run the relevant Keil baseline/inspect and migration-apply files. Other already-passing migration/model/generation tests are reused unless changed dependencies create a concrete trigger.
3. Each owner returns full code SHA, complete changed paths, exact commands/exits/evidence roots and fixture-vs-product classification. No self-acceptance or push. The independent reviewer compares the complete docs-head-to-code-head diffs and runs only disputed or independently necessary focused checks. Primary reconciles both and any remaining Probe-review findings before integrating/deploying once.
4. Task7 hardware will use an accepted installed candidate. These corrections do not touch flash/Probe/Monitor/boot code, so its evidence remains valid across unchanged paths. Do not rerun T9, original P3/P4, G/H or hardware merely for timestamp/report/fixture corrections. Add the historical timestamp limitation to final lineage and assess actual consumer dependencies explicitly.
5. Update Task11's software matrix and final complete-range coverage using the final bytes and retained evidence. Main remains cleanup owner; existing policy-rejected cleanup holds are preserved without bypass. Then resume final campaign bundle/return/acceptance. A second unsuccessful review round on the same issue returns to design.
