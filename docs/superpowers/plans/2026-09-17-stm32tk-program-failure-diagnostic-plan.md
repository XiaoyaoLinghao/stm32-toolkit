# Programming failure diagnostic slice

Specification: [approved scope and frozen contract](../specs/2026-09-17-stm32tk-program-failure-diagnostic-design.md). Accepted base `d02a605fb4b43691f03cdccc28c418c0fc0de869`; slice ID `p4-program-diagnostic-20260917`. User authorized local repair; no hardware, installation or remote operations.

1. Primary commits this plan/spec, creates a clean implementation worktree under `D:\codex-tmp\p4err-0917\impl` from that exact plan head, branch `codex/p4-program-diagnostic`. One Luna/max owner implements the complete path, without recursive delegation.
2. Owned production files under `tools/stm32-toolkit/src/stm32_toolkit`: `probe/backend.py`, `probe/pyocd_backend.py`, `probe/worker.py`, `testing/target.py`; only if verified necessary, the existing `probe/service.py`, `probe/flash.py`, `testing_workflows.py` propagation boundary. The owner must report a required expansion before editing elsewhere. Owned tests are the existing corresponding backend/worker/flash/physical-target/public-workflow files and their existing fakes. No new scripts or framework.
3. Reproduce missing diagnostic assertions first, then implement and run focused relevant modules. Use existing CPython 3.12.10 (`py -3.12`), pytest 8.4.2; deployed candidate lacks pytest and remains untouched. Run from the actual worktree root with its Toolkit/Monitor src in PYTHONPATH, PYTHONDONTWRITEBYTECODE=1, TEMP/TMP and all generated output under the approved run root. Give each run a distinct short basetemp under that root; disable pytest cache. Capture exact command, source head, exit and full failures in run logs. Fakes/injection are OFFLINE ONLY.
4. Commit product/tests, return CodeHead and evidence to primary. Primary creates a clean detached review worktree under `D:\codex-tmp\p4err-0917\review`, reviews the entire accepted-base-to-CodeHead diff, and runs only meaningful affected verification not already proven. Correctable findings return to the same owner; two failed rounds require redesign.
5. After acceptance, integrate locally and record result in `docs/codex/returns/2026-09-17-stm32tk-program-failure-diagnostic.md`; primary updates the relevant standard-test-procedure diagnostic requirement/current checkpoint. No package, deployment, flash or remote action. Preserve original P3/P4 evidence; primary cleans only attributed disposable test output after retaining minimum useful evidence.

Relevant lessons applied: STM32TK-EL-005 and EL-006 (provisional) preserve consumed-action terminality and owned-worker cleanup; GL-003/004 preserve one implementation owner, independent review and proportional verification. No lock/deadline/lifecycle redesign is intended. Existing procedure and P4 terminal-failure report were reviewed before planning tests.
