# STM32 Toolkit 0.7–1.0 Project Creation and Release Acceptance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add deterministic VS Code-first STM32 project creation and prove the complete AI-assisted migrate/build/flash/debug/monitor/test workflow on real hardware before declaring 1.0.

**Architecture:** Project creation is adapter-driven. STM32CubeMX is the primary source generator for vendor startup/HAL/LL code, while Toolkit owns validated project metadata, reproducible CMake presets, editor/AI configuration, provenance, and generated-file drift checks. A versioned acceptance harness records immutable evidence for both migrated Keil inputs and newly created projects.

**Tech Stack:** Python 3.10+, STM32CubeMX 6.18+ command-line interface, CMake/Ninja, GNU Arm Embedded Toolchain, CMSIS packs as an explicit CMSIS-only fallback, pytest, GitHub Actions, the 0.4 Probe Service, and the 0.5–0.6 monitor/test/diagnostic packages.

## Global Constraints

- Creation is VS Code/GCC-first. Do not create, update, or synchronize Keil project files.
- Do not hand-maintain an exhaustive MCU database. Obtain target data through versioned adapters and retain source/version provenance.
- Never overwrite user-owned source silently. Generated regions require ownership metadata, manifest hashes, and drift detection.
- Generated `.vscode` files must remain usable by the Claude Code extension and ordinary VS Code tooling; Toolkit distribution remains GitHub-only.
- GitHub-source installation remains the only documented distribution path.
- Any unsupported MCU, CubeMX version, pack, toolchain, debug probe, or transport must fail with a concrete remediation message.
- Hardware-skipped tests cannot satisfy the 1.0 release gate.

---

## Task 1: Add CubeMX-backed project creation and `.ioc` import

**Files:**
- Create: `tools/stm32-toolkit/src/stm32_toolkit/generation/__init__.py`
- Create: `tools/stm32-toolkit/src/stm32_toolkit/generation/models.py`
- Create: `tools/stm32-toolkit/src/stm32_toolkit/generation/cubemx.py`
- Create: `tools/stm32-toolkit/src/stm32_toolkit/generation/cmsis_pack.py`
- Create: `tools/stm32-toolkit/src/stm32_toolkit/generation/project.py`
- Create: `tools/stm32-toolkit/src/stm32_toolkit/generation/manifest.py`
- Create: `tools/stm32-toolkit/tests/fixtures/cubemx/fake-cubemx.ps1`
- Create: `tools/stm32-toolkit/tests/test_cubemx_adapter.py`
- Create: `tools/stm32-toolkit/tests/test_generation_manifest.py`
- Create: `tools/stm32-toolkit/tests/test_create_project.py`
- Create: `skills/init-stm32-project/SKILL.md`
- Modify: `tools/stm32-toolkit/src/stm32_toolkit/cli.py`
- Modify: `tools/stm32-toolkit/src/stm32_toolkit/mcp_server.py`
- Modify: `tools/stm32-toolkit/src/stm32_toolkit/project.py`
- Modify: `schemas/stm32-project.schema.json`
- Modify: `tools/stm32-toolkit/src/stm32_toolkit/schemas/stm32-project.schema.json`

**Interfaces:**
- `CubeMXAdapter.probe_version() -> ToolVersion`
- `CubeMXAdapter.generate(request, output_dir) -> GenerationResult`
- `ProjectGenerator.create_from_ioc(ioc, destination, options)`
- `ProjectGenerator.create_from_mcu(mcu, destination, options)`
- CLI/MCP: `project create`, `project import-ioc`, `stm32_create_project`

- [ ] Write failing contract tests for CubeMX script and quiet invocation, tool/version detection, paths containing spaces/non-ASCII text, cancellation, timeout, nonzero exit, and incomplete output.
- [ ] Write failing tests for new-project input validation: MCU, board or `.ioc`, HAL/LL choice, clock ownership, linker memory map, language, RTOS, output path, and explicit overwrite policy.
- [ ] Write failing manifest tests proving generated/user-owned files are distinguished, hashes detect drift, regeneration preserves user regions, and mismatched generator versions require review.
- [ ] Run focused pytest suites and record RED.
- [ ] Implement the CubeMX adapter using generated script files and argument arrays, with sanitized logs and captured CubeMX version/provenance.
- [ ] Implement `.ioc` import and MCU-based generation. Use CMSIS packs only when the user selects the CMSIS-only path; record pack identity and license metadata.
- [ ] Reuse the 0.3 managed CMake, CMake Presets, VS Code, project schema, build-manifest, and validation components rather than maintaining a second template system.
- [ ] Generate a project-local `CLAUDE.md` that documents commands and safety boundaries without embedding machine-specific absolute paths or global state.
- [ ] Implement dry-run plans, collision detection, transactional staging, atomic activation, and rollback on generation failure.
- [ ] Run unit/integration tests with fake CubeMX and a real CubeMX smoke test; archive the exact script, logs, version, and generated manifest.
- [ ] Commit: `git commit -m "feat(project): add CubeMX project creation"`

## Task 2: Build a versioned end-to-end acceptance harness

**Files:**
- Create: `acceptance/README.md`
- Create: `acceptance/schema.json`
- Create: `acceptance/scenarios/keil-to-gcc.yaml`
- Create: `acceptance/scenarios/new-cubemx-project.yaml`
- Create: `acceptance/runner.py`
- Create: `acceptance/adapters/board.py`
- Create: `acceptance/adapters/user_checkpoint.py`
- Create: `tests/acceptance/test_schema.py`
- Create: `tests/acceptance/test_fake_vertical.py`
- Modify: `.github/workflows/ci.yml`
- Modify: `tools/stm32-toolkit/src/stm32_toolkit/__init__.py`

**Acceptance record:** scenario version, Toolkit/component versions, source revision, project fingerprint, migration/generation provenance, compiler/debug-tool versions, board/MCU/probe unique identities, firmware identity/CRC, step timestamps, commands, user authorizations, monitor assertions, test results, diagnostic evidence, and artifact hashes.

- [ ] Write failing schema tests requiring every acceptance step and identity field; reject records with skipped hardware steps for the `release-1.0` profile.
- [ ] Write a failing fake vertical test that executes the exact real-board state machine and exercises recovery from a deliberate defect.
- [ ] Run the focused tests and record RED.
- [ ] Implement an idempotent runner with resumable checkpoints, per-step timeouts, lease cleanup, target-state cleanup, artifact signing/hashing, and a machine-readable final verdict.
- [ ] Implement two scenarios: a one-time Keil→GCC conversion and a new CubeMX project. Keep the initial fixtures representative rather than claiming universal Keil compatibility.
- [ ] Require this sequence for both scenarios: detect/configure → generate or migrate → build → flash → observe monitor variables/registers → inject or enable a known defect → collect logs/fault/test/history evidence → diagnose → apply a source fix → rebuild/reflash → run target tests → verify monitor assertions.
- [ ] Add CI for schema, fake adapters, package installs, and software-only vertical tests; isolate hardware jobs behind explicit self-hosted runner labels and secrets.
- [ ] Add a documented user checkpoint before irreversible or intrusive operations while allowing the AI client to autonomously perform safe observations and explain its next hypothesis.
- [ ] Run the fake vertical workflow twice concurrently with distinct workspaces and verify there is no data, port, lease, or artifact crossover.
- [ ] Commit: `git commit -m "test(acceptance): add versioned vertical harness"`

## Task 3: Harden packaging, installation, security, and compatibility

**Files:**
- Create: `SECURITY.md`
- Create: `CHANGELOG.md`
- Create: `LICENSE` (confirm the existing package metadata's MIT choice with the maintainer before adding the repository-level text)
- Create: `docs/compatibility.md`
- Create: `docs/troubleshooting.md`
- Create: `scripts/build-release.ps1`
- Create: `tests/install/test_github_install.ps1`
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `README.md`
- Modify: `README_zh-CN.md`
- Modify: `skills/setup-stm32-env/SKILL.md`
- Modify: `tools/stm32-toolkit/src/stm32_toolkit/doctor.py`
- Modify: `tools/stm32-toolkit/pyproject.toml`
- Modify: `.github/workflows/ci.yml`

- [ ] Write failing install tests for a clean Windows user profile: clone from a pinned GitHub tag, install Python packages/UI assets, register the local plugin source, open two projects, and run setup/doctor without cross-project state.
- [ ] Write failing upgrade tests across all supported schema/component versions and verify downgrade refuses incompatible state without data loss.
- [ ] Write failing security tests for loopback exposure, token redaction, path traversal, symlink/junction escapes, command injection, malicious Keil/XML/SVD/ELF inputs, stale leases, and audit tampering.
- [ ] Run installation, migration, and security suites and record RED.
- [ ] Implement a single compatibility matrix for Toolkit, plugin manifest, schema, Probe Service, monitor UI, CubeMX, compiler, Python, Node, VS Code extensions, MCU families, probes, and test transports.
- [ ] Update `setup` and `doctor` to select only required VS Code extensions: C/C++ (`ms-vscode.cpptools`), CMake Tools (`ms-vscode.cmake-tools`), and Cortex-Debug (`marus25.cortex-debug`, including SVD peripheral view); detect conflicts and provide manual remediation instead of modifying unrelated user settings.
- [ ] Document GitHub-only installation and pinned-tag upgrades in both READMEs; remove wording about public extension-registry publication.
- [ ] Build reproducible release archives/wheels with bundled monitor assets, checksums, SBOM, licenses, and no development caches or secrets.
- [ ] Run a dependency/license/security review and resolve all release-blocking findings.
- [ ] Commit: `git commit -m "chore(release): harden GitHub distribution"`

## Task 4: Execute the non-skippable 1.0 release gate

**Files:**
- Create: `docs/releases/1.0.0-acceptance.md`
- Create: `docs/releases/1.0.0-known-limitations.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/compatibility.md`

- [ ] Select and record at least one supported STM32 board/probe pair plus one real legacy Keil project and one new CubeMX project; do not broaden compatibility claims beyond archived evidence.
- [ ] Execute both complete acceptance scenarios with no hardware skips and archive all signed manifests, logs, monitor exports, test reports, firmware hashes, and diagnostic sessions outside the repository's normal source history.
- [ ] Confirm a human can review and reproduce why the AI chose each diagnostic action, what evidence contradicted alternatives, what authorization was granted, and how the fix was verified.
- [ ] Run two projects concurrently and prove workspace data, monitor groups, probes, leases, ports, tokens, and artifacts remain isolated.
- [ ] Run `pytest -q`, UI unit/E2E tests, type/lint checks, package/install tests, `claude plugin validate .`, and clean-machine GitHub install verification.
- [ ] Review every unchecked roadmap item; either complete it or move it to a versioned post-1.0 plan with an explicit limitation. Do not silently drop requirements.
- [ ] Update version to `1.0.0`, freeze schemas/protocol compatibility, finalize release notes and known limitations, and create the signed release commit/tag only after every release gate is GREEN.
- [ ] Commit: `git commit -m "release: prepare STM32 Toolkit 1.0.0"`

## 1.0 Exit Criteria

- [ ] A fresh GitHub-source installation can migrate one legacy Keil project and create one new STM32 project for GCC/VS Code without writing Keil files.
- [ ] Claude Code can use Toolkit's Skills/MCP tools to build, flash, monitor, control a debug session, collect logs/faults/tests, reason from evidence, apply a fix, and verify it on real hardware.
- [ ] All monitor groups are user-created; no project or Toolkit preset appears in a fresh workspace.
- [ ] Hardware ownership and project data remain isolated under concurrent use, with operation-level safety checks and complete audit trails.
- [ ] Compatibility claims, limitations, install/upgrade paths, version migrations, and rollback behavior are documented and backed by archived acceptance evidence.
