# VS10-A completion execution plan

Approved scope: the user set the goal to complete VS10-A and explicitly authorized necessary hardware flashing, testing and reading, including retesting after an error is captured and the related defect repaired. Do not request repeated authorization for those actions. This does not authorize remote push/PR/merge/tag/release. It does not require repeating hardware whose relevant evidence remains valid.

The primary conversation owns design, integration, dispatch and acceptance regardless of model name. Luna/max owns product implementation and implementation tests. Existing references to Sol leadership mean the primary role; implementers cannot accept their own changes. Preserve one owner per responsibility and stop a failed operation before diagnosis, correction and a justified retest.

## Frozen scenarios and boundaries

1. Complete the approved T10 retrospective supplementary assessment using original authenticated P3/P4 transcripts. Preserve the original RESOLVED Diagnostic, PASSED FixVerification, completed v3 and physical evidence. Do not relabel the supplement as a pre-fix action or create another physical pass.
2. Reconcile the original VS10-A clauses against exact source, firmware, environment and evidence identities. Fill only concrete remaining gaps, using offline verification first and necessary real hardware only when the missing fact requires it.
3. Produce the formal campaign evidence bundle and implementation return, then complete independent review of the entire original accepted-base-to-final-head range and issue an evidence-backed verdict. Stop at VS10-A; do not start VS10-B.

Original complete-range base: `9b7839bb01f88a9e3d2c13203f38aa0d11647232`. Current T10 correction base: `f65302c72817a8f4746bcc363ff60f3c4cbffb57`. Current integration checkpoint: `ad87d4df0190594ab017600caa01f2261cc28013`; this is a review checkpoint, not an accepted deployment. Canonical checkout remains `D:\codex-tmp\stm32tk-integration`. Approved run root and cleanup owner remain `D:\codex-tmp\t10h-0917` and the primary. Existing cleanup holds outside this run are untouched.

## Execution and evidence ownership

- Finish the current bounded T10 correction. Independent review found provider I/O error misclassification, MCP/core reference-validation drift, and missing leaf-model transcript-ID binding. The same Luna owner corrects these, using existing functions and tests. Already passing copy integration remains recorded; the corrected candidate must pass targeted affected checks and a fresh full-store read before deployment.
- Run one accepted candidate deployment and the prepared production offline assessment chain. Record exact source/runtime, per-command inputs/results/revisions, and preservation of all 131 pinned original files and the prior runtime state. No need to repeat G/H, T9 or original physical windows for this correction.
- In parallel, audit remaining original clauses and review non-overlapping full-diff partitions: probe/hardware/read lifecycle; project/intake/Target-v2/IDE; continuation/diagnostic evidence; Monitor/adapters/deployment; specifications/reports. Each partition returns its exact file coverage and actionable findings. Primary reconciles complete coverage and reviews final deltas after fixes; no partition verdict alone accepts VS10-A.
- Use existing passing tests until related bytes, dependencies, environment or contracts change. Task 11's final matrix follows its written risk triggers. Fixtures, replay and software checks remain distinct from physical evidence.
- Build the canonical campaign bundle with a sorted SHA-256 manifest and explicit evidence attribution, then reconcile Task 11 and Task 12. A report-format correction does not repeat product or hardware verification.

The operational ledger, command records and handoff are under the approved run root. The tracked final return must identify CodeHead before its report commit; it must not claim its own future commit SHA. Remaining uncertainty or deferred external evidence prevents claiming the corresponding clause complete.
