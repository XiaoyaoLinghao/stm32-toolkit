# Probe lifetime correction plan

Governing design: [attachment lifetime corrections](../specs/2026-09-18-stm32tk-vs10a-probe-lifecycle-corrections-design.md). Base `06d16455706d1d37a6398dcfc98ece1ea555bf85`. This plan prepares the two reported findings; begin implementation after the Probe partition review is reconciled. Existing Task7 run-entry infrastructure corrections and Target/Keil work retain their owners and evidence.

1. Primary reconciles the final Probe finding list against the design. Create one clean isolated worktree at the exact design checkpoint under `D:\codex-tmp\t10h-0917\pf`, branch `codex/vs10a-probe-final-corrections`. Do not reuse or mutate the frozen whole-range review checkout.
2. One Luna/max owner handles this coherent Probe slice. After the original owner explicitly released the untouched `pf` worktree, primary assigned `/root/probe_lifetime_impl` exclusively; `/root/hypothesis_impl` retained the independent Task7 entry and returned Target/Keil corrections. Neither is an orchestration intermediary. The Probe owner is not alone in the repository and must preserve others' edits. No recursive delegation, hardware or remote Git mutation. Owned product files are `probe/service.py`, `probe/pyocd_backend.py`, the design's minimal internal `probe/worker.py` terminal-state property and their existing focused tests. No lockfile/schema/runtime changes.
3. Record minimal RED for the cached-success-after-read-error sequence and invalid replacement identity tuple. Implement invalidation inside the serialized backend boundary and retain the previous complete identity until the existing replacement close boundary. Verify the affected positive paths and error/cleanup ordering with existing tests only. Fix test harness/environment failures before attributing them to product code. Retain commands, all output and exits; set all three temp variables and short explicit basetemps/cache beneath the run root. Primary owns cleanup.
4. Return exact CodeHead, complete diff and focused evidence. Independent reviewer examines the full base-to-CodeHead correction; implementer does not accept its own changes. Same-issue non-convergence after two rounds returns to design. Primary integrates only accepted corrections and reviews the integration delta.
5. Build/deploy one final candidate together with the other accepted product corrections. Reuse the valid deployment procedure and installed-byte/PyOCD launcher checks. Execute one finite normal Probe attach/typed activity-read/cleanup smoke under the active user authority, with exact final runtime and firmware pins. No physical failure injection or duplicate historical T9/T10 cycle. Archive final identities, corrected-path evidence and unchanged-evidence applicability in Task11/12.

No additional user permission is needed for these authorized local corrections, deployment or bounded hardware checks. Push/PR/merge/tag/release remain unauthorized. This plan does not declare the still-running whole-range review or VS10-A accepted.
